# AGENTS.md — гайд для ИИ-агента (Codex и др.), продолжающего проект

Этот файл — точка входа для любого ИИ-ассистента, который подхватывает разработку. Прочти его
целиком, затем `docs/STATUS.md`, прежде чем что-либо делать. Здесь: что за проект, как устроена
методология ведения, где что лежит, как запускать/чинить и как продолжать строго по рельсам.

---

## 1. Что за проект

Делаем **персонального ИИ-агента «под ключ»** для клиента (Mironov / Mika). Это не просто бот —
это агентная оболочка **Hermes Agent** (NousResearch, `github.com/NousResearch/hermes-agent`),
настроенная под клиента и расширенная навыками.

**Что агент должен уметь (итоговая цель проекта):**
- Telegram-бот: ввод **текст + голос**, ответы по смыслу.
- **Долгосрочная выборочная память** о пользователе (факты, привычки, договорённости).
- Свой **стиль общения** («кореш/друг» на «ты», тёплый, но дельный).
- Решение задач по **коду и текстам**: мелкий код → блоком в чат, крупный → файлом; генерация
  **КП (.docx)** и **отчётов (.csv)**.
- **Автопостинг в соцсети** (гибкий сценарий, «Вариант 3»): по свободной команде/описанию/ссылке
  (в т.ч. Tilda) агент готовит **3 версии текста** + **картинку** (генерация/обработка фона), сам
  уточняет *куда* и *когда*, и публикует через **PostMyPost**; отложенный постинг — по cron.

**Заказ и объём** — см. переписку с клиентом: `C:\Users\GigaChat\Downloads\ЗАКАЗЫ\ии_агент_перс\переписка.txt`
(не в git). Бюджет ~20-25к, внедряем поэтапно. Выбран «Вариант 3» (с генерацией фото).

---

## 2. Технологический стек и ключевые ограничения

| Что | Чем |
|---|---|
| Агентная оболочка | Hermes Agent v0.17.0 (Python 3.11), установлена в `%LOCALAPPDATA%\hermes` |
| LLM («мозг») | **OpenRouter** → `deepseek/deepseek-v4-pro` |
| Голос (STT) | **Groq API** (`whisper-large-v3-turbo`) — по API, НЕ локально |
| Анализ фото (vision) | **OpenRouter** (мультимодальная модель) — по API |
| Память | Встроенная файловая hermes (`USER.md` + `MEMORY.md`) — без БД |
| Генерация картинок | **kie.ai** (`gpt-image-2-text-to-image`) — добавляем кастомно (этап 4) |
| Публикация | **PostMyPost API** (тариф «Продвинутый 5») |
| Контент-таблица | **Postgres** (только для постинга, этап 4) |
| Автопостинг | **cron-скрипт** без LLM (этап 6) |

> ⚠️ **ГЛАВНОЕ ОГРАНИЧЕНИЕ**: агент деплоится на VPS **1 ядро / 1 ГБ ОЗУ**. Поэтому —
> **никакого локального инференса**. STT и vision только по API. Локальный faster-whisper
> исключён намеренно. Любые новые тяжёлые модели — только через API.

> ⚠️ **OpenAI НЕ используем** — у проекта нет ключа OpenAI. Только OpenRouter (+ Groq для STT).

API-доки клиента (в git, без секретов): `kie_ai_api.txt`, `postmypost_api.txt`.

---

## 3. Где что лежит

**Репозиторий проекта** (он же git-репо, рабочая папка): `D:\claude\хермес`
- `docs/STATUS.md` — снимок текущего состояния (читать ПЕРВЫМ).
- `docs/07_ROADMAP.md` — все 8 этапов с критериями приёмки (чекбоксы).
- `docs/01_HERMES_CAPABILITIES.md` — карта возможностей hermes (что встроено / что кастомим).
- `CLAUDE.md` — контекст проекта + ритуал старта.
- `AGENTS.md` — этот файл.
- Секреты НЕ в git: `доступы.txt`, `env_пример.txt`, `переписка.txt` (в `.gitignore`).

**Установка hermes** (конфиг и данные, ВНЕ git): `%LOCALAPPDATA%\hermes\` =
`C:\Users\GigaChat\AppData\Local\hermes\`
- `.env` — секреты (OpenRouter, Groq, Telegram). **Менять тут ключи → нужен рестарт gateway.**
- `config.yaml` — модель, STT (`stt.provider: groq`), vision (`auxiliary.vision.provider: openrouter`),
  toolsets, память.
- `SOUL.md` — личность + поведенческие правила (персона, вывод кода, память). **Менять → достаточно
  `/new` в чате** (перечитывается новой сессией, рестарт не нужен).
- `memories/USER.md`, `memories/MEMORY.md` — долгосрочная память.
- `logs/gateway.log`, `logs/agent.log`, `logs/errors.log` — логи.
- Код hermes: `%LOCALAPPDATA%\hermes\hermes-agent\`.

**Реальные доступы клиента**: `доступы.txt` (VPS root, OpenRouter, kie.ai, PostMyPost, прод-бот).

---

## 4. Методология ведения (okplan) — соблюдать строго

Прогресс держится на **трёх слоях правды** с приоритетом. Если противоречат — верь нижнему:

1. **`docs/STATUS.md`** — компактный снимок (легче всего устаревает).
2. **Чекбоксы в `docs/07_ROADMAP.md`** — `[ ]`/`[x]` по каждому критерию приёмки.
3. **Git-теги `stage-N-done`** — машинная правда о закрытии этапа.

**Приоритет: тег > чекбоксы > проза STATUS.** Нет тега `stage-N-done` → этап N НЕ закрыт.

### Ритуал старта сессии (обязательно)
1. Прочитать `docs/STATUS.md`.
2. `git tag -l "stage-*"` — сверить с таблицей в STATUS.
3. `git log --oneline -10` — что менялось последним.
4. Открыть `docs/07_ROADMAP.md` на текущем этапе.

### Взятие этапа в работу
1. В STATUS: обновить «Текущий этап» + «Активная работа», статус 🚧 в таблице.
2. Отдельный коммит: `chore(status): start stage N`.

### Закрытие этапа (порядок важен — тег ПОСЛЕДНИМ)
1. Все чекбоксы критериев в ROADMAP отмечены `[x]` и **реально проверены человеком** (живой тест).
2. Зафиксировать конкретику верификации (что проверили, что в логах/файлах).
3. Коммит `chore(status): close stage N`, затем `git tag stage-N-done -m "..."`.
4. В STATUS: ✅ в таблице, заполнить «Закрывающий тег», «Последний коммит», «Дата»; добавить
   запись в «Историю закрытий»; указать новый текущий этап.

> **Самопроверочный хэш в таблице**: после `git commit` хэш фиксируется; если используешь
> `--amend`, хэш меняется — впиши финальный хэш отдельным маленьким коммитом, чтобы поле
> «Последний коммит» совпадало с тегированным коммитом (см. как это сделано в истории git).

### Антипаттерны (методология не прощает)
- Тег/галочка **наперёд** (до живой проверки).
- STATUS как свалка (он — снимок на экран; детали → ROADMAP, git log).
- Закрывать этап без человеческой верификации.

### Принцип работы с человеком
**После каждого этапа — мини-отчёт человеку и человеческая проверка функционала** (где возможно —
прямо в Telegram). Claude/агент НЕ закрывает этап сам. Человек = Анастасия (разработчик).

---

## 5. Текущий статус (на 2026-06-29)

**Закрыто 4 из 8 этапов** (теги `stage-0-done`, `stage-1-done`, `stage-2-done`, `stage-3-done`):
- ✅ **0 — Каркас**: hermes установлен, бот отвечает в Telegram.
- ✅ **1 — Живой мозг**: текст (DeepSeek), голос (Groq STT), фото (OpenRouter vision), вывод кода,
  стиль «кореш/друг».
- ✅ **2 — Долгосрочная память**: встроенная файловая, сохранение + recall работают.
- ✅ **3 — Код и тексты**: `business-documents` генерирует `.docx` КП и `.csv` отчёты, Telegram UAT
  подтверждён.

**Сейчас — этап 4** (в работе): навык «контент» — свободное описание/ссылка/Tilda → 3 версии текста
и картинка через kie.ai → превью → сохранение черновика в Postgres. Уже добавлен локальный skill
`content-studio`, миграция `migrations/0001_content_drafts.sql` применена в локальной Docker-БД
`mydb`, схема `hermes_agent`, таблица `content_drafts`; тестовая запись helper вернула draft `id=2`.
Этап НЕ закрыт: ещё нужна живая Telegram-проверка с реальным kie.ai и превью.

**Осталось**: завершить stage 4, затем 5 (PostMyPost), 6 (cron-автопостинг), 7 (деплой на VPS).
Детали и критерии — в `docs/07_ROADMAP.md`.

Всегда сверяй этот раздел с реальным `docs/STATUS.md` — он актуальнее.

---

## 6. Как запускать и чинить hermes

Путь к бинарю (PATH может быть не подхвачен в свежей сессии):
`%LOCALAPPDATA%\hermes\hermes-agent\venv\Scripts\hermes.exe`

- **Запустить бота**: `hermes gateway --accept-hooks` (long-running, polling). Лог — `logs/gateway.log`,
  ждать строку `Press Ctrl+C to stop`.
- **Веб-панель**: `hermes dashboard --no-open` → http://127.0.0.1:9119 (сессии, логи, чат, правка
  `.env`/конфига). На localhost без авторизации.
- **Разовый запрос (CLI-тест без Telegram)**: `hermes chat -q "вопрос" -Q --max-turns N`.
  Использует тот же конфиг/SOUL/память — удобно для проверки механизмов.
- **Рестарт gateway после правки `.env`/`config.yaml`** (обязателен — читаются при старте). Чисто
  гасить только gateway, не задев дашборд:
  ```powershell
  Get-CimInstance Win32_Process -Filter "Name='hermes.exe' OR Name='python.exe'" |
    Where-Object { $_.CommandLine -match 'gateway' -and $_.CommandLine -notmatch 'dashboard' } |
    ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
  ```
  затем снова `hermes gateway --accept-hooks`.
- **Правка `SOUL.md`** — рестарт НЕ нужен, достаточно `/new` в чате.
- Telegram игнорирует `/start` (служебный пинг) — это by design, не баг. Для теста писать обычный текст.

---

## 7. Важные решения и подводные камни (не наступи повторно)

- **OpenRouter-ключи бывают двух типов.** Ключ клиента из `доступы.txt` (`cedd…`) —
  **provisioning/management** (`is_management_key: true`): им НЕЛЬЗЯ делать инференс (даёт
  `401 "User not found"`), только управлять ключами. Для инференса нужен обычный ключ
  (можно сгенерить из provisioning через `POST https://openrouter.ai/api/v1/keys`). Сейчас в
  `.env` стоит dev-ключ Анастасии (`0870…`, рабочий). **На прод нужен инференс-ключ аккаунта клиента.**
- **Боты: dev vs prod.** Локально — `@NEIRO_BRAIN01_BOT` + id Анастасии `2042819654`. Прод —
  `@Mironov_AgentBot` + Mika `5183294551`. Переключение на этапе 7 (деплой). Прод-значения — в
  комментариях `.env` и в `доступы.txt`.
- **DeepSeek подмешивает китайские иероглифы** в память/текст. В `SOUL.md` есть правило «память
  только по-русски». Если всплывёт в других местах — добавляй аналогичные инструкции.
- **Память**: явные «запомни» нужно сохранять немедленно (правило в SOUL.md), иначе при коротком
  диалоге + `/reset` факт теряется до флаша.
- **Hermes skills: установка и кэш.** Исходник stage-3 skill лежит в репо:
  `hermes-skills/productivity/business-documents/`; установленная копия — в
  `%LOCALAPPDATA%\hermes\skills\productivity\business-documents\`. После правки/установки skill
  перезапускай **только gateway**, иначе Telegram может жить со старым skill-index. Dashboard можно
  не трогать. Проверка: `hermes chat -q "Загрузи skill business-documents через skill_view..." -Q --max-turns 3`.
- **Stage-4 skill `content-studio`.** Исходник:
  `hermes-skills/marketing/content-studio/`; установленная копия:
  `%LOCALAPPDATA%\hermes\skills\marketing\content-studio\`. Helper `scripts/content_studio.py`
  умеет `extract-url`, `generate-image`, `save-draft`, `prepare`. Для локального Docker Postgres без
  Python-драйвера используй:
  `CONTENT_PSQL_COMMAND=docker exec -i postgres16 psql -U postgres -d mydb -At -v ON_ERROR_STOP=1`.
- **Контент-таблица stage 4.** Локально Postgres в Docker: контейнер `postgres16`, БД `mydb`,
  отдельная схема проекта `hermes_agent`, таблица `content_drafts`. Миграция:
  `migrations/0001_content_drafts.sql`. Не пиши таблицы stage 4 в `public`, там могут быть другие проекты.
- **Локальный Hermes venv сейчас сломан.** `hermes --version` и полный путь к `hermes.exe` падают с
  `No Python at "C:\Users\GigaChat\AppData\Local\Programs\Python\Python311\python.exe"`. Перед живой
  Telegram-проверкой stage 4 нужно починить/переустановить Hermes venv или опереться на уже запущенный
  gateway, если он ещё работает.
- **Hermes `platforms` во frontmatter.** Не добавляй `platforms: [linux, macos, windows]` в локальные
  skills без проверки: в текущем Hermes это распарсилось как строка и `skill_view` вернул
  `Skill 'business-documents' is not supported on this platform`. Если OS-gate не нужен — лучше
  вообще не указывать `platforms`.
- **Файлы в Telegram.** Gateway умеет отправлять локальные `.docx`/`.csv` как документы, если в ответе
  есть `MEDIA:<абсолютный путь>` без бэктиков. В CLI/TUI `MEDIA:` не использовать — там нужно просто
  напечатать путь. Поддержка расширений есть в gateway (`.docx`, `.csv` в allowlist).
- **Hermes terminal approval.** В `hermes chat` terminal-команды могут блокироваться approval-механикой
  или упираться в окружение (`uv`, venv). Для stage-3 helper в skill прописан путь через `execute_code`;
  это надёжнее, чем просить агента ставить `python-docx` или запускать произвольный terminal.
- **CSV “красота” как в образце.** CSV не хранит стили, цвета, ширины колонок и объединения ячеек.
  Красивый вид делается структурой строк: заголовок, пустые строки, секции, буллеты и отдельные
  таблицы. Образец для stage 3: `C:\Users\GigaChat\Downloads\Затирки.xlsx - Litokol Litochrom 1-6.csv`.
  В `business-documents` для таких отчётов используй `sections`, а не одну плоскую `columns/rows`
  таблицу. Section-based CSV по умолчанию пишет UTF-8 BOM + запятую, как в образце; legacy flat CSV
  оставлен с `;`.
- **DOCX без зависимостей.** На VPS 1 ГБ не тянуть лишние пакеты ради простого КП. Stage-3 helper
  собирает минимальный Office Open XML через стандартную библиотеку Python; CSV тоже генерится
  стандартной библиотекой.
- **Playwright Chromium не установлен** (ECONNRESET при инсталле) — браузерные инструменты не
  работают. Доустановить при необходимости: `cd %LOCALAPPDATA%\hermes\hermes-agent && npx playwright install chromium`.

---

## 8. Безопасность и git-конвенции

- **Секреты НЕ коммитить.** `.gitignore` исключает `доступы.txt`, `env_пример.txt`, `переписка.txt`,
  `.env`. Реальные ключи живут в `%LOCALAPPDATA%\hermes\.env` (вне репо). В коде/доках — не вписывать
  ключи (этот файл их тоже не содержит).
- **Коммиты статуса — отдельно от кода**: `chore(status): start/close stage N`. Чекбоксы можно
  отмечать вместе с реализующим кодом.
- Перед закрытием/деплоем — никаких тегов и галочек «на словах».

---

## 9. Деплой (этап 7, ориентир)

VPS в `доступы.txt` (root-доступ). hermes ставится официальным инсталлятором или через Docker
(`docker-compose.yml` в репо hermes). На сервере: переключить на прод-бот/ключ, поднять Postgres
для контента, перенести cron. Секреты — только в серверном `.env`. На 1 ГБ ОЗУ держать всё лёгким
(API-инференс, без локальных моделей).

---

**TL;DR для старта**: прочитай `docs/STATUS.md` → `git tag -l "stage-*"` → открой текущий этап в
`docs/07_ROADMAP.md` → работай по протоколу из раздела 4 → после этапа дай человеку проверить в
Telegram, и только потом тег.
