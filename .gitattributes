# Нормализация переводов строк. Критично для *.sh на Linux —
# CRLF из Windows ломает shebang/запуск на сервере.
* text=auto eol=lf

# Бинарники — не трогать.
*.png binary
*.jpg binary
*.jpeg binary
*.docx binary
*.xlsx binary
