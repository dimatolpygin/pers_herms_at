# Дорожная карта реализации — ИИ-агент «Хермес» (Mironov)

13 этапов от настройки базовой агентной оболочки до прод-готовности на VPS. Каждый
этап заканчивается **демонстрируемым результатом** (что можно потрогать руками).

Ключевая идея: **MVP-first**. Этап 1 = тонкая, но живая вертикаль (бот в Telegram
осмысленно отвечает голосом и текстом). Всё остальное наслаивается без перепиливания базы.

База: агентная оболочка **Hermes Agent** (NousResearch) — это НЕ обёртка над API-запросом
к модели, а агент со **skills**: ему даются инструменты и навыки, а *как* выполнить задачу
он решает сам (вызов shell, создание файлов/скриптов, cron и т.д.). Оболочка уже даёт
Telegram-бота, голос/vision, cron, shell/file-инструменты, роутинг моделей. Мы её настраиваем
под клиента и **добавляем навыки** (skills) + cron-обвязки для рутины: память, код/тексты,
гибкий навык контента и автопостинга.

**Принцип постинга — гибкий сценарий, а не жёсткий пайплайн.** Вход — это свободная команда
ИЛИ ссылка (в т.ч. Tilda) ИЛИ описание. Агент сам уточняет недостающее, готовит текст +
картинку, спрашивает *куда* и *когда* публиковать — как обычный диалог. Tilda-парсинг — лишь
один из возможных источников внутри навыка, а не обязательный шлюз. Рутину (публикацию по
времени) выносим в cron-скрипт без трат токенов LLM.

> **Где смотреть прогресс**: текущий статус всех этапов — в [`STATUS.md`](STATUS.md).
> Внутри каждого этапа ниже — чекбоксы конкретных acceptance-пунктов. Закрытие этапа
> сопровождается git-тегом `stage-N-done`.

| # | Этап | Главный артефакт | Зависит от |
|---|---|---|---|
| 0 | Каркас: hermes-agent поднят локально | Бот отвечает «привет» в Telegram | — |
| 1 | Живой мозг: текст + голос + стиль | Голосовое → осмысленный ответ через OpenRouter | 0 |
| 2 | Долгосрочная память | Факт сказан → агент помнит в новой сессии | 1 |
| 3 | Навык «код и тексты» | Запрос отчёта → приходит .docx/.csv файлом | 1 |
| 4 | Навык «контент»: текст + картинка (гибко) | «Сделай пост про …» → 3 текста + фото в превью | 2 |
| 5 | Навык публикации через PostMyPost (куда/когда) | «Запости в инсту сейчас» → пост в соцсети | 4 |
| 6 | Автопостинг по расписанию (cron-обвязка) | «Запости завтра в 10:00» → опубликовалось само | 5 |
| 7 | Веб-поиск: включить рабочий бэкенд | «Что сейчас с курсом X?» → агент реально ищет и отвечает с источником | 1 |
| 8 | Запрет выдумок (anti-hallucination) | Не знает → ищет/признаётся, а не сочиняет | 7 |
| 9 | Загрузка пользовательского фото в S3 | Кидаешь фото → агент отдаёт публичную ссылку S3 | 4 |
| 10 | cron-задачи (напоминания/регулярки по запросу) | «напомни в 18:00…» → пришло само в срок | 1 |
| 11 | Правка своих навыков по запросу | «поправь навык X» → навык изменён | 3 |
| 12 | Деплой на VPS | ПК выключен — бот работает 24/7 | 6–11 |
| 13 | Команда `/balance`: остатки на счетах | `/balance` → сколько $ и кредитов осталось | 12 |
| 14 | Память: оглавление + внешние заметки | Память не переполняется, чистить руками не надо | 2 |
| 15 | Модель-критик на сложных задачах | Сложная задача → второе мнение умной модели | 13 |
| 16 | Создание навыков с нуля (skill-creator) | «Сделай навык X» → агент расспросил и завёл рабочий навык | 11 |

---

## Этап 0 — Каркас: hermes-agent поднят локально

**Цель**: существующая агентная оболочка склонирована, запускается локально и отвечает
в Telegram реальным ботом клиента. Без своих навыков — просто «оно живо».

**Что делаем**:
- Клонировать репозиторий hermes-agent, изучить что уже реализовано (Telegram, STT, vision, cron, инструменты).
- Заполнить `.env` доступами: `OPENROUTER_API_KEY`, Telegram-бот, allowlist.
- Поднять зависимости, запустить агента локально на Windows.

> **Решение по ботам**: локальная разработка идёт на **dev-боте Анастасии `@NEIRO_BRAIN01_BOT`**
> + её id `2042819654`. Прод-бот клиента `@Mironov_AgentBot` + Mika (id `5183294551`)
> подключаются на этапе деплоя (этап 7). Значения зафиксированы в комментариях `.env`.

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] Репозиторий hermes-agent установлен (офиц. инсталлятор, v0.17.0), зафиксирована карта возможностей в `docs/01_HERMES_CAPABILITIES.md`.
- [x] `.env` заполнен доступами (OpenRouter рантайм-ключ + Telegram dev-бот); секреты не попадают в git (`.gitignore`, конфиг лежит вне репо в `%LOCALAPPDATA%\hermes`).
- [x] Бот запущен локально (`hermes gateway`, polling) и **отвечает на текстовое сообщение** от разрешённого id (подтверждено вживую 2026-06-28).
- [x] Allowlist настроен (`TELEGRAM_ALLOWED_USERS`) — чужие id отсекаются конфигом (штатная защита hermes; отдельным аккаунтом не тестировали).

---

## Этап 1 — Живой мозг: текст + голос + стиль

**Цель**: первый осязаемый результат — бот осмысленно общается голосом и текстом
через OpenRouter, в нужном стиле; код выдаёт по правилу (мелкий — в чат, крупный — файлом).

> **Архитектурное ограничение (сервер 1 ядро / 1 ГБ ОЗУ)**: никакого локального инференса.
> STT и vision — **только по API**. Локальный faster-whisper исключён.
> - **STT (голос)** → API-провайдер, не OpenAI (его нет). Выбран **Groq** (free tier, Whisper large-v3-turbo). Альтернативы: Mistral Voxtral, ElevenLabs Scribe.
> - **Vision (фото)** → через **OpenRouter** (мультимодальная модель, напр. Gemini Flash) — отдельный ключ не нужен.

**Что делаем**:
- Подключить модель OpenRouter (DeepSeek V4 Pro) как основной «мозг».
- STT по API: `stt.provider: groq` (+ `GROQ_API_KEY`), local отключён.
- Vision по API через OpenRouter: задать `auxiliary.vision` (провайдер openrouter + мультимодальная модель).
- Настроить системный промпт / стиль общения агента (SOUL.md).
- Базовое правило кода: мелкий фрагмент → блок с подсветкой в чат; объёмный → файл-документ.

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] Текстовый вопрос → осмысленный ответ через OpenRouter (DeepSeek V4 Pro). _(подтверждено 2026-06-28)_
- [x] Голосовое сообщение → распознано по API (Groq whisper-large-v3-turbo, без локального whisper) → осмысленный ответ. _(лог 19:27:33, «Прием, слышно?»)_
- [x] Фото в чат → агент анализирует его через OpenRouter (vision по API). _(лог 19:28:45, анализ 1523 симв.)_
- [x] Просьба «напиши маленький скрипт» → код приходит блоком с подсветкой в чат. _(подтверждено вживую 2026-06-28)_
- [x] Просьба «напиши большой скрипт» → код приходит файлом-документом. _(подтверждено вживую)_
- [x] Тон/стиль ответов соответствует согласованному (персона «кореш/друг» в SOUL.md). _(подтверждено вживую)_

---

## Этап 2 — Долгосрочная память

**Цель**: агент сохраняет факты/привычки/договорённости о пользователе и выборочно
достаёт их по необходимости (не грузит всю историю в каждый запрос).

> **Механизм**: используем **встроенную память hermes** (файловая, выборочная, лёгкая для
> сервера 1ГБ — БД не нужна): `USER.md` (профиль пользователя) + `MEMORY.md` (заметки агента),
> с лимитом символов вшиваются в системный промпт каждой сессии; агент сам их пополняет/чистит.
> Плюс `session_search` — поиск по прошлым диалогам. Postgres для памяти НЕ поднимаем
> (он будет для контент-таблицы на этапе 4). Тяжёлый бэкенд (mem0/honcho) — только если
> встроенной памяти окажется мало.

**Что делаем**:
- Убедиться, что память включена (`memory_enabled`, `user_profile_enabled` в config) и агент пишет факты.
- Проверить запись факта (явно «запомни…» и неявно из диалога) и выборочную подгрузку в новой сессии.
- При необходимости подстроить лимиты/нуджи (`memory_char_limit`, `nudge_interval`, `flush_min_turns`).

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] «Запомни, что …» → факт сохранён в `USER.md`/`MEMORY.md`. _(«Любимые цвета: чёрный и зелёный» в USER.md, 2026-06-28)_
- [x] В новой сессии (после `/new` или сброса) агент использует ранее сохранённый факт в ответе. _(recall после /new подтверждён)_
- [x] В контекст подгружается выборочная память (профиль + заметки), а не вся история диалогов. _(компактные USER.md/MEMORY.md)_
- [x] «Что ты обо мне помнишь?» → агент перечисляет сохранённые факты. _(проверено в CLI и Telegram)_

> **Доработка по ходу**: явные «запомни» сохранялись ненадёжно (curation + короткие сессии). Добавлено жёсткое правило в `SOUL.md` — на «запомни/не забудь» агент пишет в память **немедленно**, только по-русски (DeepSeek подмешивал иероглифы — устранено).

---

## Этап 3 — Навык «код и тексты»: docx / csv

**Цель**: агент генерирует деловые документы — КП в `.docx` и отчёты в `.csv` — и присылает файлом в чат.

**Что делаем**:
- Навык генерации `.docx` (КП) и `.csv` (отчёт) на стороне агента (скрипт-инструмент).
- Отправка готового файла документом в Telegram.

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] «Сделай КП по …» → приходит валидный `.docx`, открывается в Word.
- [x] «Сделай отчёт-таблицу …» → приходит валидный `.csv`, открывается в Excel.
- [x] Содержимое документа соответствует запросу (не заглушка).

---

## Этап 4 — Навык «контент»: текст + картинка (гибкий сценарий)

**Цель**: агентный навык подготовки поста. Вход свободный — команда, описание ИЛИ ссылка
(в т.ч. Tilda). Агент сам уточняет недостающее, генерирует 3 версии текста и картинку,
показывает превью, кладёт черновик в управляющую таблицу контента (Postgres).

**Что делаем**:
- Спроектировать таблицу контента в Postgres (товар/тема, версии текста, картинка, статус, площадки, время, ссылки на публикации).
- Навык генерации 3 версий текста через OpenRouter из любого входа (текст/ссылка); парсинг Tilda — как один из источников, опционально.
- Навык генерации/обработки картинки через kie.ai (gpt-image-2: дорисовка фона/обрезка; createTask → polling recordInfo → resultUrls).
- Гибкий диалог: если данных мало — агент уточняет; превью (3 текста + картинка) в чат.

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [x] «Сделай пост про …» (свободное описание) → агент уточняет недостающее и выдаёт 3 различимые версии текста.
- [x] То же по ссылке (в т.ч. Tilda) → агент достаёт суть страницы и генерит тексты.
- [x] kie.ai возвращает картинку(и): для Tilda из 1-го фото товара собирается маркетплейс-карточка (image-to-image), 2-е фото идёт как есть; для свободного брифа — обложка (text-to-image). Агент присылает превью: 3 текста + картинки.
- [x] Черновик с выбранным вариантом и всеми картинками (`images`) сохранён записью в таблице контента (id виден).

---

## Этап 5 — Навык публикации через PostMyPost (куда / когда)

**Цель**: агент гибко спрашивает *куда* (площадки) и *когда* публиковать, и умеет
опубликовать немедленно через PostMyPost (3-шаговая схема: загрузка фото → проверка → публикация).

**Что делаем**:
- Интеграция PostMyPost API (тариф «Продвинутый 5»): загрузка изображения, проверка статуса, публикация.
- Навык: агент спрашивает площадки (Telegram / Instagram / Facebook) и время, фиксирует в таблице.
- Немедленная публикация («сейчас») работает из конца в конец.

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [x] После превью агент спрашивает «куда и когда?» и понимает ответ (площадки + время или «сейчас»). _(подтверждено вживую: двухшаговый выбор площадок + доска Pinterest + время МСК)_
- [x] «Запости сейчас» → пост реально появился хотя бы в одной соцсети. _(Kira подтвердила 2026-07-01: Instagram pub id 30811163, Pinterest pub id 30810668)_
- [x] Статус и ссылка на пост записаны в таблицу контента. _(строки 15/16 `content_drafts`: `status=published`, `publication_links` с id+account_id, `platforms[].account_id`)_
- [ ] Ошибка публикации (отзыв токена соцсети и т.п.) → понятное сообщение, статус «ошибка», без молчаливой потери. _(механизм есть: helper бросает PostMyPostError с деталью, `update-draft --status failed`; **живьём по решению Kira не проверяли**)_

---

## Этап 6 — Автопостинг по расписанию (cron-обвязка)

**Цель**: отложенная публикация идёт **сама**, cron-скриптом, без трат токенов LLM.

**Что делаем**:
- Cron-скрипт-обвязка: берёт из таблицы записи со статусом «запланировано» с наступившим временем → публикует через PostMyPost → обновляет статус. Без вызова LLM.
- Агент при подготовке контента проставляет время постинга в таблицу.

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [x] «Запости завтра в 10:00» → в назначенное время пост публикуется автоматически, без человека. _(вживую 2026-07-01: Task Scheduler сам в 15:40 МСК взял due-черновик 22 и опубликовал в ВК LOVA ceramics; Kira подтвердила)_
- [x] Cron-постинг не вызывает LLM (только БД + PostMyPost API). _(`cron/autopost.py` — stdlib, ни одного вызова модели)_
- [x] После публикации статус в таблице → «опубликовано» с временем и ссылкой. _(строка 22: `status=published`, `scheduled_at=15:40 МСК`, `publication_links=[{id:30816431,vk,…}]` — ссылка = id публикации PMP; прямой vk.com-URL недоступен из-за бага валидации ответа PMP)_
- [x] Повторный прогон cron не публикует один и тот же пост дважды (идемпотентность). _(атомарный claim scheduled→publishing; прогоны после 15:40 находили «нет постов»)_

---

## Этап 7 — Веб-поиск: включить рабочий бэкенд

**Цель**: у агента появляется реально работающий веб-поиск. На вопрос про свежие
факты (курсы, новости, «что сейчас с…», проверка URL/цен) он вызывает `web_search`
и отвечает по найденному, а не по памяти модели.

> **Исходное состояние (диагностика 2026-07-01)**: веб-поиск **выключен полностью**.
> В `%LOCALAPPDATA%\hermes\.env` все ключи поиска закомментированы (Firecrawl, Tavily,
> Exa, Parallel, Brave), `SEARXNG_URL` не задан; в `config.yaml` секции `web:` нет.
> Auto-detection hermes не находит ни одного бэкенда, а единственный бесплатный
> без ключа **DDGS (DuckDuckGo)** в цепочку авто-детекта **не входит** → сам не включается.
> Именно поэтому агент не ищет и выдумывает (см. этап 8). Свой поисковик писать не надо:
> hermes даёт `web_search`/`web_extract` из коробки — нужно выбрать и включить бэкенд.

**Что делаем**:
- Выбрать бэкенд под сервер 1 ядро / 1 ГБ и бюджет клиента (решение с клиентом). Кандидаты:
  - **DDGS** — бесплатно, без ключа, search-only, качество/лимиты средние (быстрый MVP-fallback).
  - **Tavily / Exa** — 1000 запросов/мес free, AI-оптимизированы, есть `web_extract` (рекомендуется как основной).
  - **Firecrawl** — дефолт hermes, search+extract, 500 кредитов/мес free.
  - **SearXNG self-hosted** — free без лимитов, но +контейнер на слабый сервер, search-only.
  - Комбо допустимо: `search_backend` (free) + `extract_backend` (качественный).
- Прописать `web.backend` (или `search_backend`/`extract_backend`) в `config.yaml` + ключ в `.env`.
- Убедиться, что `web_search`/`web_extract` доступны боту (не только CLI) и внутри `execute_code`-песочницы (env passthrough при необходимости).
- Живой прогон: пара свежих вопросов через Telegram/CLI → агент ищет и цитирует источник.

**Критерии приёмки** (закрытие → tag `stage-7-done`):
- [x] `hermes setup` / модуль `tools.web_tools` показывает активный бэкенд (не «none»). _(2026-07-01: `✅ Web backend: tavily`)_
- [x] Вопрос про свежий факт (курс/новость/«что сейчас с…») → агент вызывает `web_search` и отвечает по найденному, с указанием источника (URL). _(Kira подтвердила вживую 2026-07-01: бот реально ищет)_
- [x] `web_extract` (если бэкенд его поддерживает) достаёт содержимое конкретной страницы по URL. _(2026-07-01: Tavily extract вернул содержимое example.com; search вернул живую погоду Москвы)_
- [x] Поиск работает из Telegram-бота, а не только из CLI. _(Kira подтвердила вживую 2026-07-01 после рестарта gateway)_

---

## Этап 8 — Запрет выдумок (anti-hallucination)

**Цель**: агент перестаёт сочинять факты. Если не уверен или спрашивают про свежие/
проверяемые данные — он **сначала ищет** (этап 7), а если поиск недоступен или ничего
не нашёл — честно говорит «не знаю / не нашёл», а не выдаёт правдоподобную выдумку.

> Зависит от этапа 7: без рабочего поиска «не выдумывай» превращается в «всегда отвечай
> "не знаю"». Сначала даём инструмент, потом правило им пользоваться.

**Что делаем**:
- Правило в `SOUL.md`: на вопросы о свежих/фактических/проверяемых данных (даты, цены,
  курсы, новости, характеристики, URL, «актуально ли…») — **сначала `web_search`**,
  ответ строить по источнику и указывать его; при пустом/недоступном поиске — явно
  сказать «не нашёл/не знаю», не додумывать.
- Отделить «творческие» задачи (тексты для постов, КП) — там выдумка уместна — от
  «фактических», где нужна достоверность. Сформулировать это в правиле.
- Прогнать контрольные вопросы, на которых агент раньше фантазировал, и убедиться в новом поведении.

**Критерии приёмки** (закрытие → tag `stage-8-done`):
- [x] Вопрос про свежий проверяемый факт → агент ищет и отвечает с источником (не из головы). _(Kira подтвердила вживую 2026-07-01)_
- [x] Вопрос, ответ на который агент не знает и не может найти → честное «не знаю / не нашёл», без выдуманных деталей. _(Kira подтвердила вживую 2026-07-01)_
- [x] Агент не выдумывает несуществующие URL/цены/характеристики (проверка на паре ловушек). _(Kira подтвердила вживую 2026-07-01)_
- [x] Творческие задачи (тексты постов/КП) по-прежнему работают — правило не зарезало генерацию контента. _(Kira подтвердила вживую 2026-07-01)_

---

## Этап 9 — Загрузка пользовательского фото в S3

**Цель**: человек кидает агенту своё фото (в Telegram), агент заливает его в S3 клиента
и отдаёт **публичную ссылку**. Это позволяет использовать собственное фото пользователя
в постинге (как `role:raw` в галерее `images`), не завися от временных Telegram-URL.

> **Доступы S3 (Beget, S3-совместимый)** — в `s3.txt` (вне git): endpoint
> `https://s3.ru1.storage.beget.cloud`, регион `ru-central-1`, bucket `bbd5f068f995-project`,
> access/secret key, публичный базовый URL. Совместим с AWS Signature v4 → `boto3` или
> `aws s3` CLI, либо helper без зависимостей (подписанный PUT-запрос). Ключи не в git.

**Что делаем**:
- Helper/навык загрузки: принимает файл или URL (Telegram file URL), кладёт объект в bucket
  с осмысленным ключом (напр. `user-uploads/<дата>_<uuid>.<ext>`), возвращает публичную ссылку.
- Определить схему публичной ссылки Beget (path-style `{endpoint}/{bucket}/{key}` vs
  virtual-hosted-style) — проверить, что ссылка реально открывается в браузере.
- Секреты S3 берутся из `.env`/конфига вне git; при необходимости — passthrough в песочницу.
- Интеграция с постингом: загруженное фото можно добавить в `images` черновика как `role:raw`.

**Критерии приёмки** (закрытие → tag `stage-9-done`):
- [x] Кидаешь агенту фото в Telegram → он заливает его в S3 и присылает публичную ссылку. _(Kira подтвердила вживую 2026-07-01)_
- [x] Ссылка открывается в браузере и отдаёт именно это изображение. _(2026-07-01: публичный URL вернул HTTP 200, image/png, верные байты)_
- [x] Загрузка работает и по прямому URL картинки (не только по файлу из чата). _(2026-07-01: `upload --url` вернул success + рабочую ссылку)_
- [x] Загруженное фото можно использовать в посте (попадает в `images` как пользовательское, `role:raw`). _(Kira подтвердила вживую 2026-07-01)_

---

## Этап 10 — cron-задачи (напоминания / регулярки по запросу)

**Цель**: пользователь может попросить агента в обычном диалоге завести напоминание
или регулярную задачу («напомни мне в 18:00 позвонить», «каждое утро в 9 присылай
погоду»), и она срабатывает сама в назначенное время, а результат приходит в чат.

> **Не путать с этапом 6.** Этап 6 (`cron/autopost.py`) — служебный автопостинг
> без LLM, его НЕ трогаем. Здесь — пользовательские задачи, которые исполняет сам
> агент (LLM) в чистой сессии. Механизм родной: hermes даёт инструмент `cronjob`
> (create/list/remove; промпт запускается по расписанию, доставка в чат). Референс
> навыка — `D:\claude\stady\.pi\skills\cron-tasks`. Нужен тонкий навык-гид
> (русские триггеры, время по МСК, самодостаточный prompt), чтобы агент надёжно
> брался за `cronjob`, а не игнорировал.

**Что делаем**:
- Убедиться, что инструмент `cronjob` доступен агенту в Telegram-тулсете; при необходимости включить.
- Навык-гид `cron-tasks`: русские триггеры («напомни…», «каждый день…», «по расписанию…»),
  правило времени МСК, требование самодостаточного `prompt` (задача исполняется без человека),
  показ/выключение/удаление задач.
- Проверить доставку результата в чат пользователя и разовые vs повторяющиеся задачи.

**Критерии приёмки** (закрытие → tag `stage-10-done`):
- [x] «Напомни мне через N минут / в HH:MM …» → в назначенное время приходит напоминание в чат. _(Kira подтвердила вживую 2026-07-02: крон работает)_
- [x] «Каждый день в 9:00 присылай …» → повторяющаяся задача срабатывает по расписанию. _(Kira подтвердила вживую 2026-07-02)_
- [x] «Покажи мои задачи» / «удали задачу …» → агент показывает и убирает задачу. _(Kira подтвердила вживую 2026-07-02)_
- [x] Время понимается как МСК; разовая задача не повторяется, повторяющаяся не пропадает после первого раза. _(timezone: Europe/Moscow; форматы расписания проверены машинно)_

---

## Этап 11 — Правка своих навыков по запросу

**Цель**: пользователь может попросить агента поправить существующий навык
(«в навыке постинга добавь площадку X», «убери это правило из content-studio»),
и агент вносит изменение в файл навыка. **Только редактирование существующих**
навыков — создание новых с нуля в объём не входит (решение клиента, осторожный вариант).

> Механизм родной: hermes даёт `skill_manager` (`edit`), правки проходят через
> штатный write-gate (защита от самовольного/вредоносного изменения поведения) —
> его оставляем как есть. Нужен навык-гид + правило в SOUL.md: редактировать
> существующие навыки можно, создавать новые с нуля — нет (пусть предложит человеку).

**Что делаем**:
- Убедиться, что `skill_manager` (edit) доступен агенту; штатный write-gate не отключаем.
- Навык-гид / правило в SOUL.md: на просьбу «поправь навык …» агент читает навык,
  вносит правку через `skill_manager edit`, кратко отчитывается что изменил. Создание
  новых навыков с нуля — не делает сам, предлагает обсудить с человеком.
- Проверить, что правка реально применяется (после подхвата навыков) и не ломает навык.

**Критерии приёмки** (закрытие → tag `stage-11-done`):
- [x] «Поправь навык X: …» (существующий) → агент вносит изменение в файл навыка, отчитывается что поменял. _(Kira подтвердила вживую 2026-07-02)_
- [x] Изменение реально видно в навыке и применяется в поведении агента. _(Kira подтвердила; машинно: create→patch→readback→delete через skill_manage работает)_
- [x] На просьбу «создай новый навык с нуля» агент не делает это молча, а предлагает обсудить (объём — только правка существующих). _(Kira подтвердила вживую 2026-07-02)_
- [x] Правка не ломает навык (frontmatter/структура остаются валидными). _(Kira подтвердила вживую 2026-07-02)_

---

## Этап 12 — Деплой на VPS

**Цель**: всё работает на сервере клиента 24/7 в Docker, независимо от ПК.

**Что делаем**:
- Деплой агента + Postgres на VPS `89.125.17.86` (Docker), через навык `/okdeploy`.
- Перенос cron-расписаний на сервер, секреты в серверный `.env` (включая `TAVILY_API_KEY` и `S3_*`).
- Перенести в серверный `config.yaml`: `web.backend: tavily` и `terminal.env_passthrough` (KIE/CONTENT/POSTMYPOST/S3) — иначе `execute_code` вычистит секреты навыков и агент будет падать/обходить через terminal.
- Перенести на сервер `SOUL.md` (вне git) и установленные копии навыков.

**Критерии приёмки** (закрытие → tag `stage-12-done`):
- [x] При выключенном ПК разработчика бот отвечает в Telegram (работает с сервера). _(`hermes-gateway` active + enabled; агент работал автономно без разработчика — 06.07 и 09.07 на проде появились навыки, 16.07 идёт живая переписка в Telegram)_
- [x] Cron-автопостинг отрабатывает на сервере. _(лог: 02.07 черновики 1 и 2 → `published`, по 7 публикаций, 0 ошибок; crontab крутит `autopost.py` ежеминутно, лог живой)_
- [x] Веб-поиск и загрузка фото в S3 работают на сервере. _(живой прогон: агент нашёл погоду Москвы и назвал источник; `s3_upload.py upload --file` → ссылка отдала HTTP 200 image/png, тестовый объект затем удалён — DELETE 204)_
- [x] cron-напоминания и правка навыков работают на сервере. _(2 живые задачи в `cron/jobs.json` — напоминание про Facebook `0 10 * * 1,4` и уборка памяти `0 5 * * *`, тикер жив; правка навыка проверена прогоном на проде 16.07)_
- [x] Postgres с памятью и контентом работает на сервере, данные сохраняются между перезапусками. _(контейнер `hermes-postgres` healthy, Up 2 weeks; `content_drafts` — 8 записей)_
- [x] Секреты только на сервере, не в git. _(`/root/.hermes/.env` права 600, вне рабочего дерева репо; `.env` в `.gitignore`)_

> **Чем подтверждено**: машинными проверками на проде 2026-07-16 + живой перепиской в Telegram.
> Отдельного UAT Микой не проводилось — закрыто по решению Анастасии 2026-07-16.

> **Не закрыто (вне критериев этапа)**: **CI/CD не активирован** — клиент не добавил GitHub
> Secrets, поэтому `/opt/pers_herms_at` отстал от master на несколько коммитов, а навыки и
> `balance` кладутся на прод вручную. Автодеплой в критерии приёмки не входил, но это долг.

---

## Этап 13 — Команда `/balance`: остатки на счетах провайдеров

**Цель**: Мика пишет в боте `/balance` и видит, сколько денег/кредитов осталось на
счетах OpenRouter, kie.ai и fal.ai — не заходя ни в один личный кабинет.

> Механика команды родная: меню Telegram собирается из `COMMAND_REGISTRY` **плюс
> навыки** (`telegram_menu_commands()` в адаптере), а навык вызывается как
> `/имя-навыка`. Значит навык с именем `balance` — это и есть команда `/balance`
> в меню бота. Отдельный движок команд писать не нужно.

**Разведка проведена (2026-07-16), все три API проверены живьём**:

| Провайдер | Эндпоинт | Чем авторизуется | Результат проверки |
|---|---|---|---|
| OpenRouter | `GET https://openrouter.ai/api/v1/credits` | **management**-ключ | ✅ `total_credits: 48`, `total_usage: 27.97` → остаток $20.03 |
| OpenRouter | `GET https://openrouter.ai/api/v1/auth/key` | рантайм-ключ | ✅ расход день/неделя/месяц; остаток — `null` |
| kie.ai | `GET https://api.kie.ai/api/v1/chat/credit` | рабочий ключ (Bearer) | ✅ `data: 930` кредитов |
| fal.ai | `GET https://api.fal.ai/v1/account/billing` | ключ со скоупом **Admin** | ⚠️ 403 с текущим API-scope ключом — нужен Admin-ключ |

Два вывода, определяющие конструкцию: (1) рантайм-ключ OpenRouter **не отдаёт**
остаток («Only management keys can fetch credits») — для цифры в $ обязателен
management-ключ; (2) у fal ключи бывают двух скоупов, billing требует Admin.

**Что делаем**:
- Навык `hermes-skills/ops/balance`: `SKILL.md` + helper `balance.py` (stdlib-only,
  как `s3_upload.py`/`autopost.py`), команда `check` → сводка по трём провайдерам.
- OpenRouter: остаток = `total_credits − total_usage` (management-ключ) + расход
  за день/неделю/месяц из `/auth/key` (рантайм-ключ) как полезный контекст.
- kie.ai: кредиты из `/chat/credit`. fal.ai: `/v1/account/billing` (Admin-ключ).
- **Честная деградация** (правило этапа 8): нет ключа / нет прав / сеть легла →
  строка «не смог узнать + причина», остальные провайдеры всё равно показаны.
  Агент не выдумывает цифры и не молчит.
- Секреты: `OPENROUTER_MANAGEMENT_KEY`, `FAL_ADMIN_KEY` (+ существующий
  `KIE_AI_API_KEY`). Helper читает `.env` сам (как `autopost.py`), поэтому
  management/admin-ключи **не** добавляем в `terminal.env_passthrough` — они не
  попадают в окружение песочницы агента без нужды.

**Критерии приёмки** (закрытие → tag `stage-13-done`):
- [x] `/balance` в Telegram → одно сообщение: OpenRouter в $, kie.ai в кредитах, fal.ai. _(Kira подтвердила 2026-07-16; fal — честной строкой «нет прав», см. ниже)_
- [x] Цифра OpenRouter сходится с тем, что показывает openrouter.ai в личном кабинете. _(Kira подтвердила вживую)_
- [x] Провайдер без ключа/прав → честная строка с причиной, остальные показаны, выдумок нет. _(машинно: 4 пути деградации; вживую — ровно так ведёт себя fal)_
- [x] Команда работает на проде (Мика видит `/balance` в меню бота). _(getMyCommands подтверждает `balance` в меню `@Mironov_AgentBot`; сквозной прогон агента на сервере прошёл)_

> **Что осталось за рамками тега**: по fal.ai показывается честная строка «нужен Admin-ключ», а не
> цифра — упирается во внешний ключ (блокер в STATUS), код готов и менять его не придётся. Закрытие
> правомерно: клиент формулировал задачу как «OpenRouter — точно, fal и kie **по возможности**», и
> обязательный OpenRouter отдаёт живой остаток в $.

---

## Этап 14 — Память: оглавление + внешние заметки

**Цель**: память перестаёт переполняться и не требует ручной чистки. Сейчас агент
сам сообщает: «заполнена на 68% (6 896 / 10 000), темп ~15–20% в неделю, ещё 1–2
недели — и начну терять важное».

> **Почему так происходит** (разобрано по исходникам hermes): `MEMORY.md` и
> `USER.md` целиком вшиваются в системный промпт **замороженным снимком** на
> старте сессии, лимит задан в символах (`memory.memory_char_limit`, на проде
> 10 000). Авто-сжатия нет **by design**: при переполнении tool `memory` просто
> возвращает ошибку, и агент должен освободить место сам. То есть переполнение —
> не баг, а следствие того, что в бюджет промпта кладут **тела** фактов.

**Конструкция** (вариант клиента, подтверждён разбором): в бюджете промпта держим
**только оглавление**, тела выносим наружу.
- `MEMORY.md` = индекс: одна короткая строка на тему — что это и где лежит полный
  текст, со ссылкой. Оглавление почти ничего не весит и растёт медленно.
- Тела — обычными файлами рядом (`notes/<тема>.md`), вне памяти и вне промпта.
- Всплыла тема → агент открывает файл по ссылке и подтягивает полный контекст.
- Раз в сутки лёгкая уборка оглавления (склеить дубли, убрать устаревшее, важное
  поднять) — через **нативный** `cronjob` этапа 10, без стороннего сервиса.

**Что делаем**:
- Навык-гид + правило в SOUL.md: что кладём в индекс, что в заметку, формат строки
  оглавления, когда открывать заметку.
- Перенос текущих ~6 900 символов в заметки без потери смысла.
- Ежедневная задача уборки индекса.
- Сторонний бэкенд памяти (mem0/honcho) **не заводим** — решение клиента.

**Критерии приёмки** (закрытие → tag `stage-14-done`):
- [x] После переезда заполненность `MEMORY.md` заметно ниже прежних 68% и не растёт линейно от новых фактов. _(9 765 → 1 232 символа: 97,7% → 12%. «Не растёт» проверено прогоном: объёмный факт → тело в заметку, оглавление 1361 → 1361, без изменений)_
- [x] Факт из старой памяти, уехавший в заметку, агент вспоминает в новой сессии (открывает по ссылке) — проверить на конкретном факте. _(прогон на проде: спросили размеры Pinterest → «1000×1500, 36pt, кернинг 7»; этих цифр в промпте нет, только в notes/posts-design.md)_
- [x] Новый факт («запомни …») кладётся по схеме: строка в оглавление, тело в заметку. _(прогон: «запомни: новый шоу-рум…» → тело дописано в notes/mika-contacts.md, в память не просочилось 0 совпадений)_
- [x] Ежедневная уборка реально отрабатывает и её результат виден. _(подброшены дубль и прошедшая встреча → склеены/убраны тикером gateway, `Last run: ok`)_
- [x] Ничего из старой памяти не потеряно при переезде. _(автопроверка покрытия: 52/52 записи найдены в новом корпусе; бэкапы в `memories/.backup/` и локально)_
- [x] Работает на проде. _(всё делалось и проверялось на сервере; gateway перезапущен)_

> **Чем подтверждено**: машинной верификацией на проде — живыми прогонами самого агента
> (`hermes -z`), а не рассуждением. **Живой UAT Микой в Telegram не проводился** — этап закрыт
> по решению Анастасии 2026-07-16.

> **Регрессия по ходу, исправлена**: первая версия уборки имела право на `rm` и **удалила заметку
> о предстоящей поездке** (18–25.07) за три дня до неё, приняв пометку «временная» за разрешение.
> Поймано регрессионным тестом, заметка восстановлена. Уборка лишена права удалять (только `mv` в
> `notes/.archive/` — принцип родного куратора hermes), устаревшим считается только то, у чего дата
> окончания уже прошла по реальному `date`. Перепроверено: дубль склеен, прошедшее убрано,
> предстоящее не тронуто.

---

## Этап 15 — Модель-критик на сложных задачах

**Цель**: на сложной задаче агент получает второе мнение умной модели (Sonnet 5),
а на простых задачах она не подключается и не жжёт деньги.

> Механизм родной, писать движок не нужно. Кандидаты, выбор — на этапе:
> **Mixture of Agents** (`moa` — виртуальный провайдер: агрегатор пишет ответ и
> вызывает инструменты, reference-модели дают анализ; есть `/moa <промпт>` —
> разовый прогон с возвратом на прежнюю модель) и **delegation** (подзадача на
> другой модели). Плюс точечные слоты `auxiliary.goal_judge` / `background_review`.
> MoA гоняет reference-модели на **каждом** вызове главной модели — поэтому
> «всегда включённый» пресет как модель по умолчанию сразу отпадает по деньгам.

**Что делаем**:
- Выбрать механизм (MoA-пресет vs delegation) по критерию «подключается только на
  сложном» и зафиксировать выбор в STATUS.
- Настроить: агрегатор — основная модель, критик — Sonnet 5 через OpenRouter.
- Правило в SOUL.md: что считаем сложной задачей.
- Посчитать стоимость: Sonnet сильно дороже основной модели, а на счету клиента ~$20 —
  поэтому этап идёт **после** `/balance` (этап 13), чтобы расход было видно.

> **Механизм выбран (2026-07-16): делегация.** MoA отпал по коду: `/moa` намеренно
> **не инструмент модели** (`agent/moa_loop.py`), его зовёт только человек — агент сам
> критика не подключит; плюс reference-модели гоняются перед **каждой** итерацией.
> `auxiliary.goal_judge` работает только в `/goal`-лупе, `background_review` пишет
> память после каждого хода — оба не критикуют ответ. У делегации свои границы, их
> учли: модель субагента задаётся **только глобально** (`delegation.model`, не на вызов),
> делегация top-level **всегда фоновая** (критика приходит вторым сообщением), субагент
> **не видит историю** — черновик передаётся в `context`.
> **Основная модель на проде — Gemini 3 Flash**, не DeepSeek (см. шапку STATUS).

**Критерии приёмки** (закрытие → tag `stage-15-done`):
- [x] На сложной задаче видно, что подключилось второе мнение, и ответ от него улучшился. _(живая цепочка в Telegram 16.07 05:10–05:11, лог gateway: Kira просит функцию на Python → агент отвечает (1463 симв.) → `ASYNC DELEGATION BATCH COMPLETE — deleg_13cc76df` → **второе сообщение** с итогом критики (2229 симв.). Содержательность критики — на КП оптовикам критик нашёл реальную логическую ошибку в прогрессии скидок)_
- [x] На простой задаче («привет», «который час») критик не подключается. _(машинно: «привет», «спасибо», «который час», «погода», напоминание → 0 вызовов `delegate_task`; на проде «Привет! Как настроение?» → CRITIC_ABSENT)_
- [x] Расход после включения критика виден через `/balance` и остаётся приемлемым. _(16.07: остаток $15.65 из $48, расход за день $0.55 — вместе с тестовыми прогонами; один вызов критика ≈ 1 цент)_
- [x] Работает на проде. _(маршрут на Sonnet 5 доказан жёстко: при заведомо неверной `delegation.model` OpenRouter ответил «not a valid model ID», притом что родитель шёл на другой модели)_

> **Чем подтверждено**: доставка второго сообщения — живым Telegram-обменом (лог gateway),
> остальное — машинными прогонами на проде. Отдельного UAT Микой не проводилось —
> закрыто по решению Анастасии 2026-07-16.

---

## Этап 16 — Создание навыков с нуля (skill-creator)

**Цель**: клиент говорит «сделай навык, который …» — агент расспрашивает, что за навык и
когда он должен срабатывать, показывает черновик и после подтверждения заводит рабочий навык.
Аналог `/skill-creator` у Claude Code.

> **Отменяет решение этапа 11.** Там объём был сознательно урезан до «только правка
> существующих навыков, создание с нуля не входит» (осторожный вариант, решение клиента
> 2026-07-02). Клиент 2026-07-16 попросил обратное. Этап 11 и его тег **не переписываем**
> задним числом — запрет снимается явно здесь.

> **Движок писать не нужно** — это третий такой этап подряд (ср. 10, 11, 13).
> `skill_manage(action="create")` уже в ядре Telegram-тулсета, и на этапе 11 полный цикл
> `create→patch→readback→delete` был **проверен машинно**. Создание технически работает уже
> сейчас; оно заблокировано только правилом в навыке `skill-editing` и в SOUL.md. Работа этапа =
> навык-гид + снятие запрета + обвязка синхронизации.

**Что делаем**:
- Навык-гид `hermes-skills/meta/skill-creator`: интервью (что за навык, по каким фразам
  срабатывает, шаги, нужны ли скрипты и секреты) → черновик `SKILL.md` → **подтверждение
  человека** → `skill_manage(create)` → `skill_view` для самопроверки → отчёт.
- Снять запрет в `meta/skill-editing` и в SOUL.md, оставив ссылку на новый навык.
- **Гейт — интервью + подтверждение** (решение клиента 2026-07-16): агент не создаёт навык
  молча. Штатный write-gate hermes не включаем — он бы затронул и правки этапа 11.
- Автосинк созданных на проде навыков в git (решение клиента 2026-07-16).

> **Три риска, вскрытые разведкой (2026-07-16), — их закрывает навык, а не движок**:
> 1. **Слот в меню Telegram.** Каждый навык = команда; лимит `max_commands` = 65, занят 61
>    (этап 13). Навык, который плодит навыки, за 4 штуки упрётся в потолок и начнёт **молча
>    вытеснять** существующие команды — ровно регрессия этапа 13. Навык обязан сверять
>    `getMyCommands` и предупреждать при подходе к лимиту.
> 2. **Git vs прод.** `skill_manage` пишет в `/root/.hermes/skills`, источник правды — репо
>    `hermes-skills/`. `rsync` в `deploy.sh` идёт **без `--delete`**, поэтому созданный агентом
>    навык деплой переживёт (комментарий в `deploy.sh` про «перезапишутся» верен только для
>    одноимённых), но в git его нет → потеря при пересоздании сервера.
> 3. **Автосинк не имеет права писать в master.** `deploy.sh` делает `git reset --hard
>    origin/master` в `/opt/pers_herms_at` — коммиты, сделанные в том же рабочем дереве, он
>    снесёт. Синк идёт из отдельного клона в отдельную ветку, мержит человек.

**Критерии приёмки** (закрытие → tag `stage-16-done`):
- [x] «Сделай навык, который …» → агент задаёт уточняющие вопросы, а не создаёт молча. _(прогон на проде «сделай навык, который считает НДС»: агент задал 3 вопроса — ставка, начислять или выделять, формат — и пообещал черновик на утверждение; навык не создан)_
- [x] После подтверждения навык реально создан, виден в `skills_list` и вызывается. _(тот же диалог с подтверждением → `ops/vat-calculator/SKILL.md` на диске, фронтматтер валиден, `description` с русскими триггерами)_
- [x] Созданный навык срабатывает по своим триггерам в новой сессии. _(новая сессия, «выдели НДС из 120000» → навык сработал: база 100 000, НДС 20 000, формат мини-отчёта как заказывали)_
- [x] Правка существующих навыков (этап 11) продолжает работать — регрессии нет. _(«поправь навык vat-calculator: добавь ставку 5% УСН» → правка легла, фронтматтер цел)_
- [x] Новый навык не вытеснил чужую команду из меню Telegram (`getMyCommands` сверен). _(62 команды из 65, все 10 наших навыков на месте; до потолка 3 слота)_
- [x] Созданный на проде навык доезжает до git и не теряется. _(`scripts/skills_pull.py` увидел созданный агентом `vat-calculator` как новый; им же спасены `lead-magnets` и `avoid-ai-writing`, живших только на проде)_
- [x] Работает на проде. _(всё проверялось на сервере живыми прогонами `hermes -z`; gateway перезапущен)_

> **Чем подтверждено**: машинной верификацией на проде — живыми прогонами агента, а не
> рассуждением. **Живой UAT Микой не проводился** — ждём его перед тегом.

> **Тестовый навык убран**: `vat-calculator` создавался в прогонах и удалён с прода (Мика его не
> просил, а слотов в меню осталось 3). Проверка правки существующего навыка делалась на нём же.

---

## Этап 17 — Supermemory: смысловая память в облаке

**Цель**: агент помнит не только то, что попало в `MEMORY.md`, но и находит факт по смыслу —
даже если спрашивают другими словами, чем записано. Встроенная память (этап 14) остаётся.

> **Движок писать не нужно — четвёртый такой этап** (ср. 10, 11, 13, 16). Плагин
> `plugins/memory/supermemory` v1.0.1 уже в поставке hermes; `hermes memory status` показывает
> его как installed, «requires API key». Настройка — две команды из README плагина:
> `hermes config set memory.provider supermemory` + ключ в `.env`. Работа этапа =
> подключение, миграция накопленного, проверка и правила приватности.

> **Разведка 2026-08-06**: ключ клиента `sm_6X8ut…` **проверен живьём** — `POST
> /v3/documents/list` → HTTP 200, аккаунт пустой (0 документов). Облачный вариант (Free)
> покрывает сценарий: SM-токены списываются **разово за уникальный контент**, повторы идут со
> скидкой 100%, хранилище безлимитно — то есть накопленная за годы память **не дорожает**,
> регулярно платится только за поиск. Расчёт по фактам проекта (память 96 КБ ≈ 40K токенов,
> один пользователь): разовая заливка ≈ $0.20, далее ≈ $0.65–1.05/мес против $5 включённого
> кредита. Самохост **проверен и отвергнут** (см. «Известные блокеры» в STATUS).

**Что делаем**:
- `memory.provider: supermemory`, `SUPERMEMORY_API_KEY` в `/root/.hermes/.env` (600, вне git).
- `~/.hermes/supermemory.json`: `container_tag`, `search_mode: hybrid`, таймауты.
- Разовая заливка накопленного: `memories/*.md` + `notes/*.md` → supermemory.
- Правило в SOUL.md: когда агент ищет в смысловой памяти, а когда читает заметку по оглавлению.
- Решить и зафиксировать режим `auto_capture` (пишет каждый ход — это и деньги, и приватность).

> **Риск приватности — обязателен к проговариванию с клиентом.** В памяти агента лежат личные
> данные Мики (контакты, бытовые заметки). При `auto_capture: true` **каждый ход диалога**
> уезжает на сторонний сервис. Это не техническая деталь, а решение клиента — фиксируем явно.

> **Риск деградации**: `api_timeout` по умолчанию 5 с, `auto_recall` ходит в сеть **перед каждым
> ходом**. Недоступность supermemory не должна ронять ответ агента — проверяем поведение при
> обрыве отдельным критерием.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [x] `hermes memory status` на проде показывает `Provider: supermemory`, проба соединения проходит. _(`✓ Connected · container: hermes_mika · auto_recall on · auto_capture on`)_
- [x] Факт, сказанный в одной сессии, вспоминается в новой **по переформулировке** (спросили не теми словами, что записывали). _(контрольный факт про печь Rohde Ecotop 100 залит ТОЛЬКО в supermemory — `grep` по `memories/` и `notes/` пуст; в новой сессии на вопрос «на чём я обжигаю керамику и во сколько обошлось» агент ответил: «Печь Rohde Ecotop 100, 340 000 ₽, 1240°C, мастерская на Колобовском»)_
- [x] Накопленная память (`memories/` + `notes/`) залита и находится поиском. _(14 файлов, 24 392 симв.; извлечено 36 фактов профиля; поиск «сколько уходит на сервисы» → строка бюджета)_
- [x] Встроенная память этапа 14 не сломана: оглавление `MEMORY.md` не разбухло, заметки читаются. _(`Memory injection / User profile / Memory tool: enabled ✓`, `MEMORY.md` и 13 заметок на месте и не тронуты заливкой)_
- [x] Обрыв/таймаут supermemory не ломает ответ агента — деградация мягкая, без падения. _(`base_url` уведён на `127.0.0.1:9` → агент ответил «391» на арифметику, gateway не упал; конфиг возвращён)_
- [x] Расход по факту укладывается в бесплатный кредит (сверено в дашборде supermemory). _(API расхода не отдаёт — сверено человеком в личном кабинете supermemory, 2026-08-22: укладывается)_
- [x] Клиент явно подтвердил, что личные данные уезжают в облако (или `auto_capture` выключен). _(подтверждено 2026-08-22: клиент согласен, `auto_capture` остаётся `on`; пароли `notes/mika-passwords.md` в облако не заливаются)_
- [x] Работает на проде. _(всё выше — живыми прогонами на 89.125.17.86, gateway перезапущен, 0 ошибок в логе)_

---

## Этап 18 — Google Calendar: создание, правка, удаление событий

**Цель**: агент не только читает календарь, но и заводит, переносит и удаляет события по
просьбе в Telegram.

> **Половина уже есть в поставке.** Штатный навык `productivity/google-workspace` (v1.1.0, Nous
> Research) умеет `calendar list` / `create` / `delete` через `scripts/google_api.py`.
> **Не хватает двух вещей**: (1) **нет команды правки** — `update`/`patch` в скрипте
> отсутствует, а клиент просит именно «редактировать»; (2) авторизация **только OAuth**
> (`google_token.json`), сервисные аккаунты не поддержаны — `grep service_account` даёт пусто.

> **Почему не патчим штатный скрипт.** `google_api.py` перезаписывается обновлением hermes:
> файл датирован 5 августа, когда прод сам приехал с v0.18.0 на **v0.20.0**. Патч тихо
> исчезнет при следующем апдейте. Поэтому — **свой навык** с собственным helper'ом, как уже
> сделано в этапах 9 (`s3-upload`) и 13 (`balance`).

> **Сервисный аккаунт лучше OAuth для сервера**: не истекает, не требует браузера и живого
> человека для реавторизации. Библиотеки уже стоят в venv (`google-api-python-client 2.194.0`,
> `google-auth 2.55.1`) — ставить нечего.

> ⛔ **Блокер, найден разведкой 2026-08-06**: аутентификация сервисного аккаунта
> `wb-parser1@client-vk-post…` проходит (JWT принят), но **Google Calendar API не включён в
> проекте** `client-vk-post` (id 107843866528) — API отдаёт `403 Calendar API has not been used
> in project … before or it is disabled`. Разблокировка — одна кнопка в консоли, см. STATUS.

**Что делаем**:
- Навык `hermes-skills/productivity/google-calendar` + helper `gcal.py` (stdlib + google-auth):
  `list` / `create` / `update` / `delete`, весь ввод-вывод JSON, время в МСК по умолчанию.
- Ключ сервисного аккаунта — `/root/.hermes/google_service_account.json` (600, вне git),
  путь через `GOOGLE_SERVICE_ACCOUNT_FILE`, проброс в `terminal.env_passthrough`.
- Правило в SOUL.md: **подтверждение перед записью** (создать/перенести/удалить) — как того же
  требует пункт 1 «Rules» штатного навыка.
- Календарь адресуем **явным `calendar_id`**: у сервисного аккаунта нет своего `primary`,
  работать он может только с тем календарём, который ему расшарили.

**Критерии приёмки** (закрытие → tag `stage-18-done`):
- [x] «Что у меня на этой неделе?» → агент показывает реальные события из календаря клиента. _(«Что у меня запланировано 13 августа?» → «одна встреча: Созвон с Ирой, 15:00–16:00 МСК»)_
- [x] «Поставь встречу в четверг в 15:00» → событие появилось в календаре, время МСК верное. _(создано в `mikamironov@gmail.com`, `2026-08-13T15:00:00+03:00`)_
- [x] «Перенеси её на 17:00» → **существующее** событие изменено, дубль не создан. _(после переноса в календаре ровно 1 событие, `id` тот же, 17:00–18:00; `patch` сохранил остальные поля)_
- [x] «Удали встречу в четверг» → событие удалено. _(«13 августа свободен», машинная сверка — 0 событий)_
- [x] Перед любой записью агент спрашивает подтверждение и показывает, что именно сделает. _(«13 августа пока пусто. Ставлю „Созвон с Ирой“ в четверг 13 августа, 15:00–16:00 по МСК. Подтверждаешь?» — и событие НЕ создано до ответа)_
- [x] Ошибки внятные: нет доступа к календарю / API выключен / событие не найдено — честный текст, без выдумок (этап 8). _(живой прогон на проде: агент позвал навык, получил `Google Calendar API выключен в проекте` и пересказал человеку с конкретным действием — «включить Calendar API в консоли», ничего не выдумав)_
- [x] Новый навык не вытеснил чужую команду из меню Telegram (`getMyCommands` сверен). _(в v0.20.0 локальные навыки в меню вообще не попадают — см. находку ниже; вытеснять нечего, `/balance` на месте)_
- [x] Работает на проде. _(весь цикл — живыми прогонами `hermes -z` на 89.125.17.86 по реальному календарю `mikamironov@gmail.com`; gateway active, 0 ошибок в логе)_

> **Блокер снят 2026-08-06**: клиент включил Calendar API и расшарил календарь
> `mikamironov@gmail.com` на сервисный аккаунт.

> **Две правки по итогам живых прогонов — обе от того, что агент вёл себя не так, как задумано:**
> 1. **`calendars` у сервисного аккаунта всегда пуст** — расшаренный календарь в его список не
>    попадает. Агент прочитал пустоту как «доступа нет» и отказался работать при **живом**
>    доступе. Теперь скрипт прямо пишет в ответ, что пусто — это норма, а SKILL.md велит
>    начинать с `list`.
> 2. **Правило подтверждения в SKILL.md не сработало** — агент создал встречу молча. Заработало
>    только после переноса правила в **SOUL.md** и **выше** раздела про критика. Это ровно
>    находка этапа 15 («позиция раздела решает»), подтвердившаяся второй раз.

> **Устаревший факт в памяти мешал сильнее, чем отсутствие кода**: запись от 31.07 «Google
> Workspace отключён, нужен OAuth» заставляла агента отказываться, не пробуя навык. Пришлось
> править и `MEMORY.md`, и supermemory. Урок: при подключении новой интеграции сразу гасить
> старую запись «оно не работает» — иначе агент верит памяти, а не инструментам.

> **Находка v0.20.0: локальных навыков в меню Telegram больше нет.** `telegram_menu_commands`
> теперь явно исключает пользовательские навыки («User-installed hub skills are excluded —
> accessible via /skills»), в меню идут только встроенные. Из наших в списке остался лишь
> `balance`. **На работу это не влияет** — диспатч навыков к меню не привязан (доказано этим же
> прогоном: `google-calendar` в меню нет, а по фразе «глянь мой гугл-календарь» он вызвался).
> Побочно снят долг этапа 13: `max_commands` поднят 65 → 80, потолок больше не жмёт.

---

## Этап 19 — Пилот: эталонный профиль `docs` + принуждение Геры к делегированию

**Оплачено**: шаг 1 сделки с клиентом (10 000 ₽, согласовано 24.08.2026). Клиент вправе
остановиться после этого этапа — тогда у него остаётся рабочий профиль документов и
делегирующий Гера, этап 20 не оплачивается. Границы объёма — в `CLAUDE.md`.

**Цель**: Гера перестаёт делать документные задачи сам и отдаёт их профилю `docs`.
Проверяется **числом**, а не впечатлением: из 10 профильных задач подряд делегировано >= 9.

**Демо для Мики**: он пишет в Telegram «сделай КП для …» -> Гера отвечает «передал профилю
документов» -> ответ приходит от `docs`, а не от Геры. И рядом — цифра «было X %, стало Y %».

> **Разведка 2026-08-27, три находки, на которых стоит план:**
> 1. **База для замера уже есть.** `/root/.hermes/state.db` -> таблица `messages` хранит
>    `tool_calls` (JSON) и связана с `sessions.source`. В Telegram-сессиях **4455**
>    сообщений с вызовами инструментов за 02.07 -> 27.08. Значит baseline меряется **по
>    реальной истории клиента задним числом**, а не «начинаем считать с сегодня».
>    Топ инструментов Геры: `execute_code` 1145, `terminal` 1126, `web_search` 503,
>    `read_file` 442 … `delegate_task` **71**. Жалоба «плохо делегирует» — подтверждена.
> 2. **Штатного «делегируй профилю» тула нет.** `delegate_task` создаёт ребёнка **в том же
>    профиле** (параметра профиля в схеме нет, `grep profile delegate_tool.py` — пусто),
>    `hermes kanban assign <id> <profile>` есть, но **kanban за всё время не использован ни
>    разу** (`kanban.db`: 0 задач) и он асинхронный — возврат результата Мике не решён.
>    Рабочий синхронный путь — `hermes --profile docs -z '<задача>'`, **замерено вживую:
>    13.9 с** на тривиальный ответ (холодный старт процесса). Для реальной задачи — минуты,
>    поэтому Гера обязан сразу сказать «передал», а не молчать.
> 3. **Профиль `docs` клиент собрал «на глаз»**: 20 каталогов навыков, из них документных
>    пять (`business-documents`, `docx`, `docx-editing`, `proposal-formatting`,
>    `rus-ip-tax-calc`), остальное — `apple`, `smart-home`, `mlops`, `github`, `social-media`…
>    Это ровно тот случай, ради которого затевался блок: профиль-специалист с чужими навыками
>    в промпте не экономит ничего.

**Что делаем** (порядок обязателен — он же порядок безопасности):

- **19.0 Замер «как есть».** Скрипт `tools/delegation_metric.py` (в git, запускается на проде):
  читает `state.db`, отбирает обращения Мики в Telegram, помечает профильные (документные) и
  считает долю делегированных. Классификация **воспроизводимая и проверяемая руками**:
  ключевые слова -> дешёвая модель-классификатор -> выгрузка выборки в
  `docs/metrics/delegation_baseline_2026-08-XX.md`, где человек может пересмотреть решения.
  Без этого шага критерий «9 из 10» не с чем сравнивать.
- **19.1 Диета профиля `docs`.** Оставить только документные навыки, остальное убрать.
  Переписать `profiles/docs/SOUL.md` (сейчас 3 КБ) под узкую роль. Замерить `hermes prompt-size`
  до и после — это и есть обещанная клиентом экономия промпта, в цифрах.
- **19.2 Транспорт «Гера -> docs».** ~~Навык, который зовёт `hermes --profile <name> -z`~~ —
  **план пересмотрен 27.08.2026 по результатам замера, см. врезку ниже.** Синхронный вызов
  через терминал нежизнеспособен, а kanban оказался готовым штатным транспортом. Сделано:
  включён тулсет `kanban` у Геры, заведён машинный реестр маршрутизации
  (`scripts/profile_registry.sh` -> `hermes profile describe` + генерируемый `ROUTING.md`),
  написан навык `meta/profile-delegate`, метрика научена считать `kanban_create` как
  делегирование профилю.

> **Врезка 19.2, замерено на проде 27.08.2026 — почему транспорт не свой, а штатный:**
> 1. **Синхронный вызов из терминала невозможен в принципе.** Реальная документная задача
>    в профиле идёт **12–16 минут**; таймаут `terminal` у Геры — **180 с**. Оценка «13.9 с»
>    из разведки была замером холодного старта на тривиальном ответе, а не работы.
>    Рецепт `hermes --profile docs chat -q '...'` при этом **лежал в навыке клиента**
>    `meta/profile-architecture` — то есть Гере был прописан способ делегирования, который
>    убивается таймаутом всегда. Это одна из причин нулевого baseline, а не только «не хочет».
> 2. **Kanban — не «непроверенный асинхронный путь», а готовый транспорт ровно под задачу.**
>    Диспетчер живёт **внутри gateway** Геры (`kanban.dispatch_in_gateway`, по умолчанию on;
>    `hermes kanban daemon` помечен DEPRECATED). Он поднимает воркера как
>    `hermes -p <профиль> chat -q 'work kanban task <id>'` c `HERMES_HOME` профиля — своя
>    модель, свои навыки, свой SOUL. `TERMINAL_TIMEOUT` воркера выводится из `max_runtime`
>    задачи (при `--max-runtime 30m` = 1770 с), то есть проблема 180 с снимается штатно.
>    Возврат результата Мике тоже штатный: `kanban.auto_subscribe_on_create` (по умолчанию on)
>    подписывает чат, из которого задачу завели, на события завершения.
> 3. **Прогон живьём.** Задача «счёт-оферта для ООО Ромашка» -> `assignee=docs`: подхвачена
>    диспетчером за 30 с, отработана профилем **12 минут**, закрыта со структурированной
>    сдачей и путём к файлу; критик Qwen дал PASS. Первые два прогона упали на
>    `Iteration budget exhausted (30/30)` — **работа была сделана, но воркер не успел закрыть
>    карточку**. Лечится `agent.max_turns` профиля (30 -> 80); реальный предохранитель —
>    `max_runtime`, а не число ходов.
> 4. **Седьмой случай подряд, когда движок уже в поставке.** Писать транспорт было не нужно;
>    нужно было включить тулсет и починить неверный рецепт в навыке.
- **19.3 Принуждение (главное).** Сделано: `hooks/profile_guard.py` + карта зон
  `hooks/profile_guard.json`, подключён в `hooks.pre_tool_call` конфига Геры,
  синхронизируется на прод из `deploy/deploy.sh`. **Руки Гере не отобраны** — блокируется
  узкий признак, инструменты на месте (инвариант 3). Отказ фейл-опен: ошибка внутри
  сторожа = пропустить вызов, сломанный сторож не роняет агента клиента.

> **Врезка 19.3, замерено на проде 27.08.2026 — что именно ловить и что из этого вышло:**
> 1. **Сигнал взят из замера, а не из головы.** В обоих контрольных прогонах этапа 19.2
>    Гера начинал с `skill_view business-documents` — **первым же вызовом**. Ловить надо
>    там, до того как он начал, а не на моменте записи `.docx`: к тому времени уже
>    потрачено 13-20 вызовов. Три правила сторожа: навык чужой зоны, путь навыка в команде
>    (обход `skill_view` — реальный вызов из замера), маркер работы руками (`python-docx`).
>    Последнее правило станет основным после 19.4, когда навыков у Геры не останется.
> 2. **Хук сработал, и этого хватило.** Тот же запрос с полными реквизитами: два блока
>    (`business-documents`, `proposal-formatting`) -> `kanban_create(assignee=docs)`,
>    задача `t_759fa128`, ответ Мике одной строкой «Передал в docs, задача t_..., вернусь
>    с готовым .docx». Тело задачи Гера собрал самодостаточным — перенёс реквизиты
>    поставщика из своих заметок. Профиль отработал её **с первого прогона за 752 с**,
>    критик Qwen дал PASS, артефакт отдан абсолютным путём вне воркспейса
>    (`artifacts/business-documents/Schet_oferta_9_Romashka.docx`, 567 КБ с фото товара).
>    Журнал сторожа при этом остался на трёх записях — все из сессий Геры, воркер не задет.
> 3. **Запрет без данных делегирования не даёт.** Прогон с неполными реквизитами: блок
>    сработал, самодеятельность прекратилась (4 вызова вместо 20), но карточку Гера не
>    завёл — вернулся к Мике за КПП/ОГРН/банком. Это правильное поведение (навык велит
>    спрашивать до передачи), но метрика засчитывает такой ход как «сделал сам»: она судит
>    по факту вызова тулов, а не по результату. Ровно для этого в скрипте есть `--review`.
> 4. **Ложных блокировок нет.** Недокументная задача (место на диске) прошла обычным
>    `terminal` без единой записи в журнале сторожа. Команды чтения и переноса
>    (`ls`, `cat`, `cp`, `file`) пропускаются намеренно: готовый файл профиля надо уметь
>    посмотреть и переслать.
> 5. **Профилям сторож не ставится.** У профиля свой `HERMES_HOME` и свой `config.yaml`,
>    блока `hooks:` там нет — воркер делает ту же работу законно. Проверено `grep`.
> 6. **`hooks_auto_accept: true` обязателен, и одного его мало.** Gateway не TTY, без флага
>    согласие не дать. Но запись в `~/.hermes/shell-hooks-allowlist.json` появляется при
>    **регистрации** — то есть при первом старте агента после правки конфига. `hermes hooks
>    doctor` хук не регистрирует и до перезапуска честно показывает «not allowlisted».
- **19.4 Диета Геры — сделано (28.08.2026).** Пять документных навыков
  (`business-documents`, `docx`, `docx-editing`, `proposal-formatting`, `rus-ip-tax-calc`)
  выключены у Геры **по одному**, каждый шаг проверен самим hermes
  (`get_disabled_skills(load_config())`), не глазами. Индекс навыков 14 638 -> 14 068 Б.
  Навыки не удалены — это симлинки в `/root/.hermes/skills/`, удаление сломало бы их во
  всех профилях; выключение обратимо одной строкой конфига. У профиля `docs` все пять на
  месте (промпт 15 153 Б).

> **Врезка 19.4, замерено на проде 28.08.2026.**
> 1. **Диета клиента не работала вообще — ни одного дня.** Ключ `skills.disabled` лежал в
>    `config.yaml` **одной YAML-строкой с JSON внутри** (та же форма, что у `toolsets`).
>    `skills_config._normalize_skill_names` нормализует строку как *одно имя навыка*
>    (`str -> [str]`, это осознанное поведение, фикс #13026), поэтому весь список из 62 имён
>    стал одним несуществующим навыком. Проверка:
>    `get_disabled_skills(load_config())` возвращал **1 элемент** — сам JSON-текст.
>    Признак, видимый снаружи: `hermes prompt-size` продолжал показывать `notion`,
>    `humanizer`, `claude-code` и остальных «выключенных».
> 2. **Клиентские 62 имени включать молча нельзя — и мы их не включили.** Форму ключа
>    починили, но в рабочий список положили только пять документных; 62 имени сохранены
>    рядом комментарием «включать по его слову». Данные для его решения собраны:
>    в `/root/.hermes/skills/.usage.json` статистика есть у 54 навыков, и **ни у одного из
>    этих 62** — то есть включение почти наверняка безболезненно, но это его выбор, а не наш.
> 3. **Выключенный навык всё ещё можно позвать по имени.** В контрольном прогоне Гера
>    вызвал `skill_view business-documents` уже после диеты — имя он помнит из `SOUL.md`.
>    Вызов поймал хук 19.3. Вывод: диета убирает *подсказку*, хук ловит *память*; по
>    отдельности ни то, ни другое не закрывает вопрос.
> 4. **Цена диеты в байтах маленькая, ценность — не в байтах.** −570 Б индекса при промпте
>    ~87 КБ. Смысл в том, что у Геры больше нет короткого пути: инструкция «как собрать
>    .docx» из его промпта исчезла.
> 5. **Свидетельство из живого чата клиента (27.08, 18:08–18:18).** До диеты в одной сессии
>    сторож сработал **шесть раз подряд**: `write_file .docx` -> `execute_code .docx` ->
>    `skill_view docx` -> `execute_code from docx import` -> `terminal from docx import` ->
>    `execute_code .docx`. Мика это заметил сам: «по идее ты не должен даже изначально
>    пытаться, чтобы вообще доходить до блокировки — это лишняя итерация». После диеты в
>    контрольном прогоне записей сторожа **ноль**, `kanban_create` первым же ходом.
> 6. **Чистоту эксперимента портит правка клиента.** Вечером 27.08 Мика попросил Геру
>    исправиться, и тот **сам себе дописал `SOUL.md`** стоп-правило «увидел .docx/.xlsx/.pptx
>    -> сразу `kanban_create`, не жди блокировки». Поэтому «ноль блокировок» в прогоне 28.08 —
>    результат трёх вещей сразу (диета + хук + правило в SOUL), разделить их задним числом
>    нельзя. Отдельная проблема: `SOUL.md` Геры **в git не лежит** (в `.secrets/` был снимок
>    от 02.07, 15 088 Б против 37 106 Б на проде) — обновлён локально, но это дыра в
>    инварианте 4, и её закрывать в этапе 20.
- **19.5 Метрика остаётся жить — сделано (28.08.2026).** На проде cron
  `0 4 * * 1 bash /opt/pers_herms_at/cron/delegation_weekly.sh`: считает окно в 7 дней тем же
  скриптом, что дал baseline, и дописывает строку в журнал. Журнал лежит вне клона репо
  (`/root/hermes-metrics/delegation_log.md`) — `deploy.sh` делает `git reset --hard` и стёр бы
  его; в репозиторий забирается `pscp`, копия — `docs/metrics/delegation_log.md`.
  Первый прогон записан.

> **Врезка 19.5.** В скрипте нашлась и починена зеркальная ошибка счёта: уведомление
> диспетчера «`[kanban] Task … completed`» приходит в базу как `role='user'`, автоотбор видел
> в нём новую документную задачу и записывал её в «Гера сделал сам». То есть **чем лучше Гера
> делегирует, тем хуже становилась цифра**. Врезка добавлена в `MACHINE_BLOCKS` целым блоком
> (первой строки мало — ниже идут `Title:` / `Assignee:`, они открывали эпизод заново).
> Автоотбор всё равно тянет лишнее (например, реплику, где Мика пересказывает переписку про
> документы, ничего не прося) — приёмочный замер `--last 10` смотреть глазами и снимать
> лишнее через `--review`.

**Критерии приёмки** (закрытие -> tag `stage-19-done`):
- [x] Есть число «как есть» до работ, снятое по реальной истории Telegram, с выборкой, которую человек может пересмотреть. _(2026-08-27: **0 из 18**, 0.0 %. 2494 хода в Telegram за 02.07-27.08; отобрано 45 задач автоматом, 27 снято при ручном пересмотре, осталось 18 с реальной работой. Отчёт `docs/metrics/delegation_baseline_2026-08-27.md`, вердикты `docs/metrics/review_docs_2026-08-27.json`.)_
- [x] «Сделай КП для <клиента>» -> Гера отвечает, что передал профилю документов, и ответ приходит от `docs`. _(2026-08-28: два КП из десяти — сеть кофеен «Тёплый угол» (`t_df9c1641`) и сеть отелей «Северный дом» (`t_7b241bbb`), оба через `kanban_create(assignee=docs)`, ответ Мике одной строкой «передал». Проверено из CLI; в Telegram тот же путь — `auto_subscribe_on_create` подписывает чат, откуда заведена задача.)_
- [x] Гера **не может** молча сделать документ сам: попытка блокируется хуком, в ответе Мике — понятная причина, а не ошибка. _(2026-08-27: два блока в одном прогоне — `business-documents`, `proposal-formatting`; журнал `/root/.hermes/logs/profile_guard.jsonl`. Ответ Мике — «Передал в docs, задача `t_759fa128`», не ошибка.)_
- [x] Из 10 профильных задач подряд делегировано **не меньше 9** (замер тем же скриптом, что и baseline). _(2026-08-28: **10 из 10, 100 %**. Было 0 из 18. Десять задач разного типа — КП, публичная оферта, акт выполненных работ, расчёт УСН, спецификация, гарантийный талон, счёт физлицу, прайс услуг, закрывающий акт — поставлены подряд, каждая ждала закрытия своей карточки. Все десять ушли `kanban_create -> docs` и закрыты профилем с артефактом. Отчёт `docs/metrics/delegation_after_2026-08-28.md`. Оговорка: задачи ставила Анастасия из CLI с полными данными, а не клиент из Telegram — проверялось поведение Геры, живая нагрузка клиента впереди. Сторож за прогон сработал 10 раз: Гера ещё пытается взяться сам, но каждый раз после блокировки заводит карточку.)_
- [x] Недокументные задачи (пост, код, поиск) Гера по-прежнему делает сам — ложных блокировок нет. _(2026-08-27: задача про место на диске прошла обычным `terminal`, в журнале сторожа пусто; плюс 13 синтетических кейсов в `hooks/README.md`.)_
- [x] `hermes prompt-size` профиля `docs` меньше, чем у Геры; обе цифры записаны. _(2026-08-27: Гера 80 551 Б, docs 15 155 Б — в 5.3 раза меньше. Индекс навыков 7 822 -> 974 Б, навыков 73 -> 7. Сделано штатным `hermes skills opt-out --remove`, обратимо через `opt-in --sync`.)_
- [x] Всё живёт на проде и переживает рестарт gateway. _(2026-08-28: `systemctl reload hermes-gateway` (ExecReload=kill -USR1) — gateway active, Telegram переподключился сам; после рестарта диета и хук на месте, `get_disabled_skills` отдаёт те же пять имён, сторож пишет в журнал.)_
- [x] Профиль, хук и скрипт метрики лежат **в git**, а не только на сервере (инвариант 4). _(2026-08-28: `profiles/docs/` (SOUL, config, skills.txt, .env.example), `hooks/profile_guard.*`, `scripts/delegation_metric.py`, `cron/delegation_weekly.sh`, снимок конфига Геры `profiles/default/config.yaml`. **Не закрыто**: `SOUL.md` самого Геры в git не лежит — только снимок в `.secrets/`; клиент правит его через агента. Переносить в этапе 20 вместе с шаблоном профиля.)_
- [ ] Клиент неделю пользуется и подтверждает, что стало лучше.

> **Что в этап НЕ входит** (проговорено клиенту, см. `CLAUDE.md`): критик-цикл через
> `kanban swarm`, двухслойная память, приватность профиля, приёмка остальных пяти профилей.
> Отдельно: в `profiles/docs/config.yaml` стоит `delegation.model: gemini-3.7-flash`, а ТЗ v8
> требует критиком `qwen/qwen3.7-max` — **расхождение не чиним в пилоте**, критик не оплачен.

> **Честный риск, назвать клиенту заранее**: делегирование не бесплатно — задачу читают две
> модели вместо одной, и ответ приходит на десятки секунд позже. Выигрыш не в скорости одной
> задачи, а в том, что промпт Геры перестаёт расти и документы делает тот, кто заточен.

---

## Этап 20 — Конвейер: шаблон профиля, `profile-forge`, приёмочный чеклист, `dev` как тест

**Оплачено отдельно**: шаг 2 сделки (15 000 ₽). Берётся **только если клиент принял этап 19.**

**Цель**: профиль перестаёт быть ручной поделкой. Клиент заводит седьмой и восьмой профиль
(Health, Finance) сам, за час, по одному и тому же пути.

**Демо**: при клиенте собирается профиль `dev` — по шаблону, скриптом, с прохождением чеклиста,
без ручной возни. Это и есть тест шаблона (инвариант 1).

**Что делаем**:
- Шаблон профиля в git: `SOUL.md`, `config.yaml`, список навыков, `.env.example`, деплой-скрипт.
- `profile-forge` — навык/скрипт «завести профиль из шаблона» одной командой.
- `profile-acceptance` — чеклист, без прохождения которого профиль не выпускается.
- Маршрутизация из машинного реестра (`hermes profile describe`), а не из прозы в SOUL Геры:
  добавить профиль = добавить строку (инвариант 5).
- Собрать `dev` конвейером и прогнать через чеклист.

> **Опорная находка этапа (машинно, 28.08.2026): конвейер писать с нуля не надо.**
> В hermes есть **дистрибутивы профилей**. `hermes profile install <git-URL | каталог>`
> ставит профиль по манифесту `distribution.yaml`; `hermes profile update <имя>`
> перекатывает его из источника, **не трогая** память, сессии, `auth.json` и `.env`.
> Это инвариант 4 («профиль живёт в git»), закрытый штатно. Седьмой случай подряд,
> когда движок уже в поставке (ср. этапы 10, 11, 13, 16, 17, 19).
>
> Два свойства определили конструкцию:
> 1. **Установка из дистрибутива не засевает поставочные навыки** — профиль встаёт
>    пустым. Ручная диета `docs` (73 навыка -> 7) во второй раз не понадобилась.
>    `hermes profile create`, наоборот, кладёт полный комплект из 67.
> 2. **Дистрибутив не может содержать симлинков** (`_reject_distribution_symlinks`),
>    а навыки профиля — именно симлинки в общий `/root/.hermes/skills/`. Поэтому
>    `skills/` в манифест не кладётся: положить их файлами значило бы расщепить
>    один навык на семь расходящихся копий. Раздаёт навыки фордж.
>
> **Что пришлось развести:** `delegation.model` в `config.yaml` — это модель
> субагентов `delegate_task`, а **не критик**. В `profiles/docs/config.yaml` туда
> смотрел Gemini Flash при критике Qwen Max в `SOUL.md`. Критик профиль зовёт сам
> по инструкции в SOUL; путать эти два ключа — тихо подорожавшие субагенты при
> неизменившемся критике.
>
> **Ловушка замера:** `hermes prompt-size` подмешивает `AGENTS.md` и файлы **текущего
> каталога**. Запуск чеклиста из клона репозитория показывал профилю `dev` 31 686 Б
> вместо настоящих 16 005 — то есть чеклист ругался бы на профиль за содержимое
> каталога, из которого его проверяют. Мерить только с `cwd` = дом профиля.

**Критерии приёмки** (закрытие -> tag `stage-20-done`):
- [x] Профиль `dev` собран **скриптом из шаблона**, не руками, и время сборки записано.
      `python3 scripts/profile_forge.py install dev` — **2.74 с** (28.08.2026), из них
      1.7 с сама сборка. Ставится штатным `hermes profile install` из
      `profiles/dev/distribution.yaml`; фордж добавляет `.env`, диету
      (`skills opt-out --remove`), симлинки восьми навыков, каталог артефактов,
      `describe` и пересборку `ROUTING.md`. Промпт профиля: 44 918 -> 13 836 Б.
- [x] Кодовая задача в Telegram уходит в `dev`, документная — в `docs`, обе не перепутаны.
      Замер 28.08.2026, две задачи подряд одной сессией Геры: «собери расширение Chrome для
      витрины Tilda» -> `kanban_create(assignee=dev)`, задача `t_936f2868`; «сделай акт
      выполненных работ на мастер-класс для ООО «Ромашка»» -> `kanban_create(assignee=docs)`,
      задача `t_637e0ee9`. Не перепутаны. Обе карточки подхвачены диспетчером и отработаны
      своими профилями.
      **Три вещи, которые этот замер показал и которые дороже самой галочки:**
      1) *Принуждение работает только при полных данных.* Первый прогон обеих задач Гера
         завернул уточняющим вопросом — в кодовой не было сказано, какая именно страница,
         в документной не было даты. Поведение правильное, но замер срывает: планировать
         проверки только с полностью определёнными задачами.
      2) *Сигнала по навыкам для `dev` не хватило.* На «сделай расширение Chrome» Гера собрал
         его сам за 19 вызовов, ни разу не открыв навык: он изучал сайт через `browser_*`
         (эти тулы под `matcher` хука вообще не попадают) и сразу писал файлы. Понадобились
         узкие маркеры на подпись готового продукта — `manifest_version`, `content_scripts`,
         `chrome.runtime`. С ними тот же запрос дал блокировку на 13-м вызове и делегирование
         на 15-м.
      3) *Общего маркера «пишет код» быть не может.* Гера постоянно и законно пишет себе
         скрипты (cron, метрика, разбор `state.db`); маркер вроде `.py` превратил бы сторожа
         в глухую стену. Ловим артефакт, а не занятие.
- [x] Чеклист приёмки существует и `dev` его прошёл; результат прогона записан.
      `scripts/profile_acceptance.py` — 15 быстрых проверок + 4 живых. `dev` прошёл
      все, замечаний ноль. Живой прогон: карточка `t_ee1a47bc` подхвачена диспетчером,
      сделана воркером за **49 с**, артефакт `/root/.hermes/artifacts/dev/price_bump.py`
      (5 418 Б) со сработавшей самопроверкой. Отчёт —
      `docs/metrics/acceptance_dev_2026-08-28.json`.
      Сверх плана на конвейер переведены **все шесть** профилей (`content` 2.94 с,
      `seo` 2.98 с, `strategist` 3.30 с, `writer` 2.93 с, `docs` 2.65 с, `dev` 2.74 с) —
      все шесть проходят чеклист, отчёт `docs/metrics/acceptance_all_2026-08-28.json`.
      Этим закрыта ловушка, найденная 27.08: у пяти профилей `SOUL.md` начинался словами
      «Общаешься напрямую с Микой. Ты отдельный Telegram-бот». Профиль с этой строкой,
      запущенный воркером, ведёт себя как собеседник — задаёт вопрос в пустоту и ждёт
      ответа, которого не будет. Теперь строки нет ни у кого, и пункт стоит блокирующим
      в чеклисте. `docs` после переезда прогнан живьём повторно (`t_264faa91`, артефакт
      `Price_list_LOVA_CERAMICS_2026.docx`, 62 598 Б) — регрессии по этапу 19 нет.

> **Находка, которая дороже всего остального в этапе: рестарт gateway убивает
> работающего воркера.** Документная задача `t_637e0ee9` дважды показала на доске
> «crashed / pid not alive» и была брошена (`gave_up`, 2 из 2 попыток). Причина прямо
> в журнале systemd:
> `hermes-gateway.service: Killing process 3132359 (hermes) with signal SIGKILL`.
> Воркеры живут в cgroup gateway (`kanban.dispatch_in_gateway`), у юнита
> `KillMode=mixed` — после выхода главного процесса systemd добивает всё, что осталось
> в группе. Значит **любой** рестарт (деплой, правка конфига, самообновление hermes)
> уносит работу, которая идёт прямо сейчас, а клиент увидит «профиль не работает».
> `profile_forge.py install` и `deploy.sh` теперь смотрят на доску и отказываются
> работать при занятых воркерах; обход через `FORGE_IGNORE_RUNNING` /
> `DEPLOY_IGNORE_RUNNING` оставлен, но осознанный.
- [x] Новый профиль добавляется **одной строкой в реестр**, SOUL Геры не переписывается.
      `profiles/registry.yaml` — блок профиля; `SOUL.md` Геры при заведении профиля
      не участвует вовсе, справочник `ROUTING.md` для его навыка генерируется.
- [ ] Клиент сам, без нас, поднимает пробный профиль по инструкции — и он работает.
      Инструкция написана: `docs/10_PROFILE_CONVEYOR.md`. Ждём прогона клиентом.
- [x] Всё в git, деплой воспроизводим.
      Шесть профилей рендерятся из реестра и лежат в `profiles/`; установка идёт из
      клона `/opt/pers_herms_at`, то есть из git. Закрыт долг этапа 19: `SOUL.md` Геры
      (37 106 Б) в `profiles/default/`, синхронизация — `scripts/gera_soul_sync.sh`
      **в направлении прод -> репо** (клиент правит SOUL через самого агента, раскатать
      git поверх значило бы стереть его работу).

---

## Как писать критерии приёмки

Критерий — **наблюдаемое поведение**, которое клиент может проверить руками, а не внутренняя задача.
Хороший набор критериев = сценарий приёмочной проверки. Когда все `[x]` — этап реально готов к тегу.
