# STATUS — текущее состояние проекта (ИИ-агент «Хермес»)

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-08-06 (заведены этапы 17 и 18 — новые правки клиента)
**Текущий этап**: **17 — Supermemory (смысловая память в облаке)** 🚧. Следом **18 — Google Calendar CRUD** ⏸ (блокер: Calendar API выключен в Google-проекте).
**Следующий шаг**: подключить supermemory на проде и залить накопленную память. Параллельно — попросить включить Calendar API (одна кнопка, ссылка в блокерах). Открытые долги (не блокеры, деталь — ниже): CI/CD не активирован (нет GitHub Secrets); нет Admin-ключа fal.ai для `/balance`; расхождение прод/репо по `content-studio` и `business-documents`; в меню Telegram осталось 3 свободных слота из 65.

> ⚠️ **Прод сам обновился: hermes v0.18.0 → v0.20.0** (2026.8.3), обнаружено 2026-08-06. Файлы
> штатных навыков переписаны 5 августа (`google_api.py` и др.). Практический вывод: **патчить
> файлы в `/usr/local/lib/hermes-agent/` нельзя** — правки исчезнут при следующем апдейте.
> Всё своё живёт в `/root/.hermes/skills` и репозитории.

> ⚠️ **STATUS врал про модель (исправлено 2026-07-16)**: с этапа 12 здесь значилось
> `deepseek/deepseek-v4-pro`, но в прод-`config.yaml` стоит **`google/gemini-3-flash-preview`**
> (`model.default`, provider openrouter). Модель меняли мимо этого файла. Память агента была права,
> STATUS — нет. Решение Анастасии: **оставить ту модель, что стоит**. Противоречащие записи из
> памяти убраны (там лежали ЧЕТЫРЕ взаимоисключающие версии). Для этапа 15 это существенно:
> критик Sonnet 5 встаёт в пару к Gemini 3 Flash.

**Прод**: hermes **нативно + systemd** на `89.125.17.86`, Postgres **Docker** (`hermes-postgres`), бот `@Mironov_AgentBot`. Репо публичный: `https://github.com/dimatolpygin/pers_herms_at`. CI/CD настроен, но **не активирован** (клиент не добавил GitHub Secrets) — деплой навыков вручную.

**Деплой 2026-07-02 (сервер `89.125.17.86`, Ubuntu 24.04, 2 ядра/2 ГБ + 2G swap):** root-пароль в `доступы.txt` был устаревшим — клиент прислал актуальный (`tWH6…`). Установлено машинно: hermes v0.18.0 (`/usr/local/bin/hermes`, дом `/root/.hermes`), gateway под systemd (`hermes-gateway.service`, active+enabled). Postgres 16 в контейнере `hermes-postgres` (порт только localhost), миграции 0001–0003 применены. Прод-`.env` (бот `@Mironov_AgentBot` `8399306789:…`, allowlist Мика `5183294551`, инференс-ключ клиента, Tavily/Groq/KIE/PostMyPost/S3) залит в `/root/.hermes/.env`. `config.yaml` пропатчен `hermes config set` (НЕ перезаписан целиком — на сервере схема v0.18 новее локальной v0.17): timezone Europe/Moscow, model deepseek/deepseek-v4-pro, web.backend tavily, stt.provider groq, vision openrouter, `terminal.env_passthrough` (KIE/CONTENT/POSTMYPOST/S3). Полный `SOUL.md` (15 КБ, правила этапов 4/5/8/9/10/11) залит. Навыки синхронизированы rsync'ом в `/root/.hermes/skills`. Cron автопостинга (`* * * * *`, venv-python, `HERMES_ENV_FILE`). **Проверено машинно:** Telegram polling connected · инференс DeepSeek V4 Pro HTTP 200 (биллинг на клиента, `is_byok:false`) · autopost `--dry-run` прошёл всю цепочку (env→docker exec psql→helpers) · postgres healthy. **Осталось:** клиент добавляет GitHub Secrets → активируется автодеплой (этап 12 закрыт и без этого — автодеплой в критерии приёмки не входил).

**Смена ключей 2026-07-02 (в `.env`, вне git):** KIE_AI_API_KEY → `18e63f…` (проверен: kie.ai recordInfo отвечает 200). OpenRouter: клиент прислал `sk-or-v1-cedd8f69…` — это **provisioning-ключ** (для инференса даёт 401 "User not found", но валиден для управления ключами). Из него **сгенерирован рабочий инференс-ключ** `sk-or-v1-2ae0…` (POST `https://openrouter.ai/api/v1/keys`, имя на аккаунте `hermes-mironov-runtime-2026-07`), проверен живым chat-запросом (DeepSeek V4 Pro ответил). Поставлен в `.env`. **Теперь инференс биллится на аккаунт КЛИЕНТА** (не на dev-ключ Анастасии 0870). **Смена ключей требует рестарта gateway.** → **Блокер прод-ключа OpenRouter снят.**

**Баг «ключ KIE не пробросился в sandbox» — ИСПРАВЛЕН (2026-07-01).** Причина: `execute_code` (`_scrub_child_env`) дропает из дочернего окружения любую переменную, не входящую в safe-префиксы, а имена с `KEY/TOKEN/SECRET/AUTH/DSN` режет в первую очередь. Секреты навыков проходили только если навык загружен через `skill_view` в той же сессии (регистрация сессионная, ContextVar) — отсюда плавающие сбои; агент обходил через `terminal` (там режутся лишь Hermes-провайдеры). Фикс: в `config.yaml` добавлен `terminal.env_passthrough` со всем набором переменных навыков (KIE_AI_API_KEY, CONTENT_*, POSTMYPOST_*, S3_*) — детерминированный проброс независимо от тайминга. Провайдер-ключи (OpenRouter/Groq/Tavily) остаются вычищены (безопасность цела). Проверено машинно через `_scrub_child_env`. **Требует рестарта gateway** (config читается при старте). На деплой (этап 10) перенести в серверный config.

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас: hermes-agent поднят локально | ✅ | `stage-0-done` | `ed4e485` | 2026-06-28 |
| 1 | Живой мозг: текст + голос + стиль | ✅ | `stage-1-done` | `fb08808` | 2026-06-28 |
| 2 | Долгосрочная память | ✅ | `stage-2-done` | `fa0fde4` | 2026-06-28 |
| 3 | Навык «код и тексты» (docx/csv) | ✅ | `stage-3-done` | `85a78e4` | 2026-06-29 |
| 4 | Навык «контент»: текст + картинка (гибко) | ✅ | `stage-4-done` | `dda0bf2` | 2026-07-01 |
| 5 | Навык публикации PostMyPost (куда/когда) | ✅ | `stage-5-done` | `a392e78` | 2026-07-01 |
| 6 | Автопостинг по расписанию (cron-обвязка) | ✅ | `stage-6-done` | `0ea5922` | 2026-07-01 |
| 7 | Веб-поиск: включить рабочий бэкенд | ✅ | `stage-7-done` | `cec95a0` | 2026-07-01 |
| 8 | Запрет выдумок (anti-hallucination) | ✅ | `stage-8-done` | `a537afb` | 2026-07-01 |
| 9 | Загрузка пользовательского фото в S3 | ✅ | `stage-9-done` | `a5fd85a` | 2026-07-01 |
| 10 | cron-задачи (напоминания/регулярки по запросу) | ✅ | `stage-10-done` | `9504e37` | 2026-07-02 |
| 11 | Правка своих навыков по запросу | ✅ | `stage-11-done` | `5cd2355` | 2026-07-02 |
| 12 | Деплой на VPS | ✅ | `stage-12-done` | `99e5c61` | 2026-07-16 |
| 13 | Команда `/balance`: остатки на счетах | ✅ | `stage-13-done` | `60afd6b` | 2026-07-16 |
| 14 | Память: оглавление + внешние заметки | ✅ | `stage-14-done` | `007dc8c` | 2026-07-16 |
| 15 | Модель-критик на сложных задачах | ✅ | `stage-15-done` | `99e5c61` | 2026-07-16 |
| 16 | Создание навыков с нуля (skill-creator) | ✅ | `stage-16-done` | `99e5c61` | 2026-07-16 |
| 17 | Supermemory: смысловая память в облаке | 🚧 | — | — | — |
| 18 | Google Calendar: создание/правка/удаление | ⏸ | — | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап 17 — Supermemory (смысловая память в облаке)** 🚧 — **развёрнут на проде, 6 критериев из
8 закрыты машинной проверкой**. `memory.provider: supermemory`, контейнер `hermes_mika`,
`search_mode: memories`, ключ в `/root/.hermes/.env` (600). Накопленная память (14 файлов,
24 392 симв.) залита, извлеклось 36 фактов профиля. Встроенная память этапа 14 цела.
**Осталось до тега — два пункта, оба на человеке**: (1) сверить расход в личном кабинете
supermemory (API расхода не отдаёт); (2) решение клиента по приватности — при
`auto_capture: true` каждый ход диалога уезжает в облако вместе с личными данными Мики.
**Пароли (`notes/mika-passwords.md`) в облако не заливались** — исключены намеренно.

**Этап 18 — Google Calendar CRUD** ⏸ — **код готов и развёрнут**, ждёт включения Calendar API.
Навык `productivity/google-calendar` (+ helper `gcal.py`: `calendars/list/create/update/delete`)
на проде, ключ сервисного аккаунта в `/root/.hermes/google_service_account.json` (600).
Живой прогон показал: агент навык зовёт, ошибку про выключенный API пересказывает честно.
После нажатия «Enable» останется узнать ID календаря (`gcal.py calendars`) и прописать
`GOOGLE_CALENDAR_ID` — у сервисного аккаунта своего `primary` нет.

**Бэкапы перед работой с памятью**: `.secrets/backups/hermes_CRITICAL_pre_supermemory.tar.gz`
(8.1 МБ, 716 файлов: `memories/`, `notes/`, `SOUL.md`, `config.yaml`, `skills/`), целостность
проверена `gzip -t`. Вне git.

Подробности по закрытым этапам — в «Истории закрытий» ниже.

**Открытые долги** (не блокеры, но помнить):
- **CI/CD не активирован** — клиент не добавил GitHub Secrets, поэтому `/opt/pers_herms_at`
  отстаёт от master, а навыки кладутся на прод вручную (rsync/scp) + рестарт gateway.
- **Меню Telegram: 62 команды из 65** — каждый новый навык съедает слот, на потолке Telegram
  молча выкинет чужую команду (регрессия этапа 13). Сверять `getMyCommands`.
- **Расхождение прод/репо шире навыков**: `scripts/skills_pull.py` показывает, что у
  `content-studio` на проде **7 файлов**, которых нет в git (шрифт GothamPro, логотип,
  референсы imagemagick / fal-pricing / model-architecture), у `business-documents` — ещё 2,
  у `postmypost` расходится `SKILL.md`. Не разбирали — нужен просмотр каждого.
- **fal.ai** — для `/balance` нужен Admin-ключ от клиента (см. «Известные блокеры»).

## Известные блокеры

- **Google Calendar API выключен — этап 18 ⏸ (найдено 2026-08-06)**: сервисный аккаунт
  `wb-parser1@client-vk-post.iam.gserviceaccount.com` **аутентифицируется успешно** (JWT принят),
  но любой вызов Calendar API отдаёт `403: Google Calendar API has not been used in project
  107843866528 before or it is disabled`. То есть ключ рабочий, а сама API в Google-проекте
  `client-vk-post` не включена. **Что нужно для разблокировки**: владельцу проекта нажать
  «Enable» на
  https://console.developers.google.com/apis/api/calendar-json.googleapis.com/overview?project=107843866528
  (~1 минута, вступает в силу за несколько минут). После этого — проверить, что нужный календарь
  расшарен на сервисный аккаунт с правом **«Внесение изменений в мероприятия»** (не «Просмотр»):
  у сервисного аккаунта нет своего `primary`, он работает только с расшаренными календарями, и
  прав на чтение для создания/правки событий не хватит.
- **Supermemory self-host — проверено и отвергнуто (2026-08-06)**: `supermemory-server 0.0.6`
  не поднимается (`Cannot find module '@rivetkit/rivetkit-wasm'`, `/api/rivet/metadata` → 500), а
  **все эндпоинты, которые нужны плагину hermes, отдают 404** — патчить `base_url` бессмысленно,
  API просто нет. Процесс с прода снят, порт 6767 свободен. Идём облачным вариантом (этап 17).
- **PostMyPost — этап 5 закрыт 2026-07-01**: аккаунт активен, доступы в `postmypost.txt` (вне git): `project_id=349678`, Bearer-токен, 7 соцсетей (ВК LOVA ceramics, 4 доски Pinterest, Instagram lova_ceramics, Telegram LOVA ceramics). Живая публикация идёт в реальные соцсети клиента — по-прежнему тестовые посты только с явного разрешения.
- **Прод-ключ OpenRouter — РЕШЕНО 2026-07-02**: инференс-ключ клиента `sk-or-v1-2ae0…` сгенерирован из его provisioning-ключа и стоит в `.env` (биллинг на клиента). См. блок «Смена ключей» в шапке.
- **fal.ai — нужен Admin-ключ (этап 13)**: баланс fal отдаётся только ключу со скоупом **Admin**
  (`GET https://api.fal.ai/v1/account/billing?expand=credits`); присланный ключ `ed9d1ac5…` —
  **API-scope**, на биллинг даёт `403 authorization_error`. **Что нужно для разблокировки**: создать
  Admin-ключ на https://fal.ai/dashboard/keys (аккаунт клиента) и положить в `FAL_ADMIN_KEY`
  (локальный `.env` + `/root/.hermes/.env`). До этого `/balance` показывает по fal честную строку
  «нет прав» — OpenRouter и kie.ai работают. Кода менять не придётся, только ключ.
- **Локальный `hermes.exe` в Codex sandbox**: без escalation CLI может падать с `No Python at "C:\Users\GigaChat\AppData\Local\Programs\Python\Python311\python.exe"`, потому что sandbox не видит базовый Python venv. Вне sandbox Hermes CLI работает: `Hermes Agent v0.17.0`, Python 3.11.9; `skill_view` видит `content-studio` (CLI session `20260629_231213_e24de7`).

_Архитектурное правило (актуально для всех этапов): сервер 1 ядро/1 ГБ → никакого локального инференса. STT=Groq API, vision=OpenRouter API (решено и внедрено на этапе 1)._

## История закрытий

2026-07-16 — Этап 16: Создание навыков с нуля (skill-creator) — tag `stage-16-done`.
**Отменяет урезанный объём этапа 11** («только правка существующих» — осторожный вариант,
решение клиента 02.07). Тег `stage-11-done` задним числом не переписывали.
**Движок писать не пришлось — третий подряд такой этап** (ср. 10, 11, 13): `skill_manage(create)`
уже в ядре тулсета и был проверен машинно ещё на этапе 11; создание блокировало **только правило**
в навыке `skill-editing` и в SOUL.md. Сделано: навык `hermes-skills/meta/skill-creator`
(интервью → черновик → подтверждение → `create` → `skill_view` → отчёт), `skill-editing` v1.1.0,
раздел «Свои навыки: правка и создание» в прод-SOUL.md (бэкап `SOUL.md.bak.stage16`, md5 сверен),
`scripts/skills_pull.py`.
**Гейт — интервью + подтверждение** (решение клиента): штатный write-gate hermes не включали, он
затронул бы и правки этапа 11.
**Синк переигран против первоначального решения клиента**: «автокоммит с сервера» невозможен —
у сервера нет write в GitHub (`could not read Username`), нужен был бы новый ключ от клиента, и он
лежал бы там, где у агента root-терминал. Заменено на скрипт **со стороны разработчика**: ходит по
SSH, коммитит в ветку `skills-from-prod`, пушит правами человека. Вышло безопаснее и без блокера.
**Главная находка: запрет этапа 11 де-факто не работал, навыки уже терялись.** На проде нашлись
два навыка, которых **никогда не было в git**: `lead-magnets` (создан 09.07, наш стиль, реально
использовался — use=1/patches=2) и `avoid-ai-writing` (сторонний, v3.11.0, **прописан в SOUL** как
обязательный для текстов LOVA — потеря сломала бы правило). Оба спасены в репо.
**Ловушка, которую обошли**: метка curator'а `agent-created` **врёт** — «72 agent-created» из 82,
включая `apple-notes`/`airtable`. Своё от штатного отличаем по `skills/.bundled_manifest`
(`hermes skills list --source local` → 10 local против 68 builtin, builtin все disabled).
**Верификация**: живыми прогонами на проде — «сделай навык, который считает НДС» → 3 уточняющих
вопроса + обещание черновика, навык **не создан**; с подтверждением → `ops/vat-calculator` создан,
фронтматтер валиден; новая сессия «выдели НДС из 120000» → сработал (база 100 000, НДС 20 000);
«поправь навык …» → правка легла (регрессии этапа 11 нет); меню **62 из 65**, никого не вытеснило;
синк увидел созданный агентом навык как новый. Тестовый `vat-calculator` с прода удалён.
Kira подтвердила вживую оба сценария. Все 7 критериев `[x]`.
**Правило на будущее**: в меню осталось **3 слота** — на потолке Telegram молча выкинет чужую
команду (так на этапе 13 пропал `/skill-editing`). Синк **односторонний** (прод → репо): слепой
`--apply` затрёт локальную правку, ещё не уехавшую на сервер — поэтому есть `--only`/`--skip`.

2026-07-16 — Этап 15: Модель-критик на сложных задачах — tag `stage-15-done`.
Настройка — две строки конфига (`delegation.provider: openrouter`, `delegation.model:
anthropic/claude-sonnet-5`) плюс раздел «Второе мнение на сложной задаче» в SOUL.md. Движок не писали.
**Доставка второго сообщения — то единственное, что оставалось на UAT, — подтверждена живым
Telegram-обменом** (лог gateway 16.07 05:10–05:11): Kira просит функцию на Python → агент отвечает
(1463 симв.) → `ASYNC DELEGATION BATCH COMPLETE — deleg_13cc76df` → **второе сообщение** с итогом
критики (2229 симв.). Ровно тот порядок, что описан правилом.
**Три находки, без которых правило не работало** (важнее самой настройки): (1) **порядок
«инструмент → текст»** — текстовый ответ завершает ход, поэтому «сначала ответь, потом критикуй»
не звало критика **никогда**; (2) **позиция раздела** — в конце 22-КБ SOUL агент правило цитировал,
но не применял («lost in the middle»), заработало после переноса в начало; (3) формулировка должна
закрывать отговорку «типовая задача, мне всё понятно».
**Деньги**: вызов критика ≈ 1 цент; `/balance` 16.07 — $15.65 из $48, за день $0.55 (вместе с
тестовыми прогонами). Маршрут на Sonnet доказан жёстко: заведомо неверная `delegation.model` →
OpenRouter «not a valid model ID», притом что родитель шёл на другой модели.
**Методическая ловушка**: `hermes sessions export` без `--session` отдаёт сессии новыми вперёд, а
критик заводит **свою** сессию — проверка по `rows[0]` даёт ложное «критик не звался».
Закрыто по решению Анастасии; отдельного UAT Микой не было. Все 4 критерия `[x]`.

2026-07-16 — Этап 12: Деплой на VPS — tag `stage-12-done`.
Сервер `89.125.17.86` (Ubuntu 24.04, 2 ядра/2 ГБ + 2G swap) развёрнут 02.07: hermes **нативно +
systemd** (`hermes-gateway.service`, active+enabled), Postgres 16 в Docker (`hermes-postgres`,
порт только localhost), миграции 0001–0003, прод-бот `@Mironov_AgentBot`, allowlist Мика.
**Верификация 16.07, машинно + живой перепиской**: бот работает автономно от ПК разработчика
(06.07 и 09.07 на проде сами появились навыки, 16.07 идёт живая переписка); автопостинг **реально
публиковал с сервера** (02.07 черновики 1 и 2 → `published`, по 7 публикаций, 0 ошибок), cron крутится
ежеминутно; веб-поиск живьём (агент нашёл погоду + источник); S3 живьём (`upload --file` → ссылка
HTTP 200 image/png, тестовый объект удалён, DELETE 204); cron-напоминания — 2 живые задачи
(Facebook `0 10 * * 1,4`, уборка памяти `0 5 * * *`), тикер жив; правка навыков на проде
проверена прогоном; Postgres healthy, Up 2 weeks, 8 черновиков; `.env` 600, вне git.
Закрыто по решению Анастасии; отдельного UAT Микой не было. Все 6 критериев `[x]`.
**Долг (вне критериев)**: **CI/CD не активирован** — клиент не добавил GitHub Secrets, поэтому
`/opt/pers_herms_at` отстал от master, а навыки и `balance` кладутся на прод вручную.
**Грабли методики**: `s3_upload.py` читает **готовое окружение** и `.env` сам не парсит (в отличие
от `balance.py`/`autopost.py`) — на проде переменные ему даёт `terminal.env_passthrough`. Запуск
helper'а «просто по ssh» даёт ложное «Set S3_ACCESS_KEY» — надо экспортировать `S3_*` руками
(`source .env` в этом `.env` ломается на `CONTENT_PSQL_COMMAND` с `docker exec -i`).

2026-07-16 — Этап 14: Память — оглавление + внешние заметки — tag `stage-14-done`.
Архитектура и разбор причины — в [`08_MEMORY_ARCHITECTURE.md`](08_MEMORY_ARCHITECTURE.md).
**Было хуже, чем в жалобе клиента**: он цитировал агента с «68% (6 896/10 000)», по факту нашли
**9 765 / 10 000 = 97,7%** (`USER.md` — 4 443/6 000 = 74%).
**Причина не в количестве фактов**: `MEMORY.md`/`USER.md` вшиваются в промпт замороженным снимком,
лимит в символах, авто-сжатия нет **by design** (tool отдаёт ошибку, агент чистит сам). Забивали
**дубли и устаревшее**: сервер записан трижды почти дословно, про основную модель — ЧЕТЫРЕ
взаимоисключающие записи, дубли про проактивность/FAL/оформление. Растила память машинерия:
`background_review` (форк после хода сам пишет память; 21 срабатывание за 2000 строк лога) + нудж
каждые 10 ходов — писали автоматом, чистить было некому. Показательно: SOUL.md уже велел вести
«безлимитный» `session_memory.md`, но агент забросил его через день (файл не менялся с 06.07) —
**правило без машинерии не работает**, поэтому уборка сделана крон-задачей.
**Решение**: в промпте только оглавление (строка-указатель на тему), тела — файлами в
`~/.hermes/notes/`, агент открывает по ссылке. Движок писать не пришлось: `read_file`/`write_file`/
`search_files` уже в ядре тулсета, оглавление живёт в штатном `MEMORY.md`. Лимит из проблемы стал
принуждением: при переполнении агент выносит тела в заметки, а не удаляет факты. Протокол — в
SOUL.md (всегда в промпте → действует и на `background_review`). Сторонний бэкенд (mem0/honcho) не
заводили — решение клиента.
**Результат**: `MEMORY.md` 9 765 → **1 232** (97,7% → 12%), `USER.md` 4 443 → **1 269** (74% → 21%),
10 261 символ тел вынесен в 10 заметок вне промпта.
**Верификация** — машинная, живыми прогонами агента на проде (`hermes -z`): (1) факт из заметки
вспоминается — спросили Pinterest, ответил «1000×1500, 36pt, кернинг 7», этих цифр в промпте нет;
(2) новый объёмный факт → тело в заметку, **оглавление не выросло ни на байт** (1361 → 1361);
(3) уборка склеила подброшенный дубль и убрала прошедшую встречу; (4) автопроверка покрытия
**52/52** записи — ничего не потеряно. **Живой UAT Микой не проводился** — закрыто по решению
Анастасии. Бэкапы: `memories/.backup/` на сервере + локально.
**Регрессия по ходу, исправлена**: первая версия уборки имела право на `rm` и **удалила заметку о
предстоящей поездке** (Карелия 18–25.07) за три дня до неё — приняла мою же пометку «временная» за
разрешение. Поймано регрессионным тестом, заметка восстановлена. Теперь уборка **не удаляет ничего**
(только `mv` в `notes/.archive/` — принцип родного куратора hermes «never auto-deletes»), а
устаревшим считает только то, у чего дата окончания прошла по реальному `date`.
**Уборка**: нативный `cronjob`, `0 5 * * *` МСК, `--deliver local` (молча, без спама Мике).

2026-07-16 — Этап 13: Команда `/balance` — остатки на счетах — tag `stage-13-done`.
Навык `hermes-skills/ops/balance` (`SKILL.md` + `scripts/balance.py`, stdlib-only) + копия в
`/root/.hermes/skills/ops/balance`. **Движок команд писать не пришлось**: навык с именем `balance`
и есть `/balance` — меню Telegram собирается из `COMMAND_REGISTRY` плюс навыки, а диспатч идёт через
`resolve_skill_command_key` независимо от меню.
**Эндпоинты (все проверены живьём)**: OpenRouter `/api/v1/credits` — остаток `total_credits −
total_usage`, **только management-ключом** (рантайм даёт `403 Only management keys can fetch
credits`); `/api/v1/auth/key` рантайм-ключом — расход день/неделя/месяц; kie.ai `/api/v1/chat/credit`
— кредиты; fal.ai `/v1/account/billing?expand=credits` — **только Admin-scope** ключом.
**Секреты**: `OPENROUTER_MANAGEMENT_KEY`/`FAL_ADMIN_KEY` лежат в `.env`, но намеренно **не** в
`terminal.env_passthrough` — helper парсит `.env` сам (приём из `autopost.py`), поэтому ключ, умеющий
плодить API-ключи на аккаунте клиента, не висит в окружении песочницы агента.
**Верификация**: машинно — 4 пути деградации (нет ключа / не тот скоуп / не management / всё
недоступно) дают честный текст с причиной, без падений и выдумок, `exit 1` только если не узнали
ничего; сквозной прогон агента на проде (`hermes -z "/balance"`) вернул живые цифры и честную строку
по fal; `getMyCommands` подтвердил `balance` в меню `@Mironov_AgentBot`. Вживую — Kira подтвердила
(цифра сошлась с личным кабинетом OpenRouter). Все 4 критерия `[x]`.
**Регрессия по ходу, найдена и починена**: меню Telegram упирается в `max_commands` (дефолт **60**);
52 встроенных + 8 наших навыков = ровно 60, и `balance` девятым вытеснил из меню `/skill-editing`
(этап 11) — подтверждено через `getMyCommands`. Поднято до 65 (`hermes config set
platforms.telegram.extra.command_menu.max_commands 65`, границы [1,100]) → 61 команда, скрытых 0.
Payload 5 687 байт: предупреждение в коде hermes про «потолок ~4 КБ» — перестраховка, регистрация
проходит. **Правило на будущее: каждый новый навык ест слот меню — сверять `getMyCommands`.**
**Не закрыто**: по fal.ai — честная строка «нужен Admin-ключ» вместо цифры (внешний блокер, см.
«Известные блокеры»); код готов, нужен только ключ. Клиент формулировал задачу как «OpenRouter —
точно, fal и kie по возможности», обязательный OpenRouter отдаёт живой остаток.

2026-07-02 — Этап 11: Правка своих навыков по запросу — tag `stage-11-done`.
Инструмент `skill_manage` — **нативный в hermes**, уже в ядре Telegram-тулсета (`skills_list`/`skill_view`/`skill_manage`), write-gate **по умолчанию выключен** (правки проходят сразу; включается оператором через `write_approval`). Добавлен навык-гид `hermes-skills/meta/skill-editing` (только SKILL.md) + правило SOUL.md: политика **только правка существующих** навыков (patch/edit), новые с нуля агент не создаёт сам — предлагает обсудить; фронтматтер не ломать.
**Верификация**: машинно (2026-07-02) — полный цикл `create→patch→readback→delete` через `skill_manage` отработал (success, изменение сохранилось, тестовый навык удалён). Вживую — Kira подтвердила: правка существующего навыка применяется, на «создай с нуля» агент предлагает обсудить. Все 4 критерия `[x]`.

2026-07-02 — Этап 10: cron-задачи (напоминания/регулярки по запросу) — tag `stage-10-done`.
Инструмент `cronjob` — **нативный в hermes**, уже в ядре Telegram-тулсета (`_HERMES_CORE_TOOLS`), включать не пришлось. Планировщик — **встроенный in-process тикер внутри gateway** (asyncio + файловый лок, `cron/scheduler_provider.py`, интервал 60с), кроссплатформенный — на Windows работает при запущенном gateway, не требует Linux cron / Task Scheduler (в отличие от `autopost.py` этапа 6). Добавлен навык-гид `hermes-skills/productivity/cron-tasks` (только SKILL.md, helper не нужен): русские триггеры, форматы расписания (`30m`/`every 30m`/cron/ISO), самодостаточный prompt, МСК. Таймзона зафиксирована явно: `timezone: Europe/Moscow` в config.yaml (была локальная сервера). Правило в SOUL.md.
**Верификация**: машинно (2026-07-02) — форматы расписания парсятся, таймзона резолвится в Europe/Moscow, `cronjob` в тулсете. Вживую — Kira подтвердила: крон работает (напоминание пришло в срок). Все 4 критерия `[x]`.
**На деплой (этап 12)**: сохранить `timezone: Europe/Moscow` (или `HERMES_TIMEZONE`) на сервере, иначе крон уедет в UTC.

2026-07-01 — Этап 9: Загрузка пользовательского фото в S3 — tag `stage-9-done`.
Новый навык `hermes-skills/media/s3-upload` (+ установленная копия `%LOCALAPPDATA%\hermes\skills\media\s3-upload`). Helper `s3_upload.py` — stdlib-only, ручной AWS Signature v4 (без boto3, requests есть но не нужен), команда `upload --file|--url`. Заливает в Beget S3, отдаёт path-style публичную ссылку `{public_base}/{bucket}/user-uploads/<дата>_<uuid>.<ext>`, объекты `x-amz-acl: public-read`. Креды `S3_*` в `.env`; правило в SOUL.md — «своё фото → s3-upload → ссылку можно в пост как role:raw». Оба файла вне git — на деплой перенести.
**Верификация**: машинно (2026-07-01) — `upload --file` и `upload --url` вернули success; публичная ссылка открылась (HTTP 200, image/png, верные байты); подпись DELETE тоже рабочая (тестовые объекты удалены, бакет чист). Вживую — Kira подтвердила: фото из Telegram → ссылка; фото использовано в посте как role:raw. Все 4 критерия `[x]`.
**На деплой (этап 10)**: перенести `S3_*` в серверный `.env`.

2026-07-01 — Этап 8: Запрет выдумок (anti-hallucination) — tag `stage-8-done`.
Опирается на рабочий поиск этапа 7. В SOUL.md (вне git, `%LOCALAPPDATA%\hermes\SOUL.md`) добавлен раздел «Достоверность и поиск»: (1) факт о мире → сначала `web_search`, ответ по найденному + ссылка-источник; (2) не нашёл/недоступно/противоречиво → честно «не нашёл/не уверен», без выдумывания URL, цен, цифр, цитат, названий, версий; (3) творчество (тексты постов, КП, слоганы, идеи) — исключение, фантазировать можно, проверять только вкраплённые факты; (4) сомнение «факт или творчество» → считать фактом. Усилён принцип честности.
**Верификация**: Kira подтвердила вживую (2026-07-01) все 4 критерия — свежий факт (агент ищет + источник), ловушка (не выдумал, признал незнание), творческая задача (генерация не пострадала). Правило применяется после рестарта gateway / `/new` (SOUL.md вшивается в системный промпт при старте сессии).

2026-07-01 — Этап 7: Веб-поиск: включить рабочий бэкенд — tag `stage-7-done`.
Диагностика показала корень проблемы: поиск был выключен полностью (в `.env` все ключи закомментированы, секции `web:` в `config.yaml` не было, DDGS не в авто-детекте) — потому агент и не искал, и выдумывал. Своё писать не пришлось: hermes даёт `web_search`/`web_extract` из коробки. Включён **Tavily** основным бэкендом (`config.yaml` → `web.backend: tavily`, ключ в `.env`); Exa + Firecrawl прописаны резервом (переключение = смена `web.backend`).
**Верификация**: машинно — `tools.web_tools` показал `✅ Web backend: tavily`, `web_search` вернул живую погоду Москвы (2026-07-01 16:30, +29.2°C), `web_extract` достал содержимое example.com. Вживую — Kira подтвердила после рестарта gateway: бот реально ищет в Telegram и отвечает по найденному. Все 4 критерия ROADMAP `[x]`.
**На деплой (этап 10)**: не забыть перенести `TAVILY_API_KEY` в серверный `.env`. Веб-поиск — нативный инструмент gateway, ключ читается при старте.

2026-07-01 — Этап 6: Автопостинг по расписанию (cron-обвязка) — tag `stage-6-done`.
`cron/autopost.py` (stdlib, **без LLM**): берёт `content_drafts` `status=scheduled` с наступившим `scheduled_at`, атомарно клеймит (scheduled→publishing, идемпотентность), публикует через переиспользуемые функции `postmypost.py` (upload/create_publication status 5) по каждому `platforms[].account_id`, write-back через `content_studio.update_draft`. Launcher `run-autopost.bat`, миграция `0003` (статус `publishing`), README со схемой планировщика (Task Scheduler локально / Linux cron на проде).
**Верификация**: Kira подтвердила вживую (2026-07-01) — Windows Task Scheduler сам в 15:40 МСК опубликовал черновик 22 в ВК LOVA ceramics (pub `30816431`), строка → `published` + `scheduled_at` + ссылка. Идемпотентность и «без LLM» — машинно. ROADMAP критерии 1–4 `[x]`.
**Баг по ходу, исправлен**: cp1251-падение `log()` на `→`/эмодзи при выводе в файл (пост уходил, но статус зависал `publishing`) → форс UTF-8 stdout/stderr; `.bat` через `%~dp0` вместо `cd` в кириллическую папку. Строку 22 финализировали вручную; чистый end-to-end прогон с фиксом живьём не повторяли (нет нужды — механизм тот же).
**Нюанс ссылки**: `publication_links` хранит id публикации PMP, не прямой vk.com-URL (`GET /publications/{id}` в PMP отдаёт 422 из-за их бага валидации ответа). После теста задачу планировщика удалили.

2026-07-01 — Этап 5: Навык публикации PostMyPost (куда/когда) — tag `stage-5-done`.
Навык `hermes-skills/social-publishing/postmypost` (helper `postmypost.py`, dependency-light) + write-back через новую команду `content_studio.py update-draft`. Диалог «куда/когда (МСК)» двухшаговый: площадки по отдельности (ТГ/ВК/Инста/Pinterest или «все сразу», без кросспостинга по умолчанию) → если Pinterest, отдельным вопросом доска (4 шт) → время МСК/«сейчас». Порядок картинок = порядок `--image-url` (карточка первой).
**Верификация**: Kira подтвердила вживую (2026-07-01) — агент запросил площадки/доску/время и опубликовал реально: Instagram (pub `30811163`), Pinterest «Вазы и кашпо» (pub `30810668`). Машинно: `content_drafts` строки 15/16 = `status=published`, `platforms[].account_id`, `publication_links` с id. ROADMAP критерии 1–3 `[x]`.
**Косяки по ходу, исправлены**: (1) write-back создавал **новую** строку вместо UPDATE → дубль-запись; добавлена команда `update-draft` (UPDATE по id, массив-безопасная дозапись ссылок). (2) `scheduled_at` не заполнялся у запланированных постов → `update-draft --scheduled-at`. Оба нужны крону этапа 6.
**Не закрыто**: критерий 4 (ошибка публикации → status failed) — живьём не проверяли по решению Kira; механизм готов.

2026-07-01 — Этап 4: Навык «контент» (текст + картинка, гибко) — tag `stage-4-done`.
Локальный Hermes skill `content-studio` (исходник `hermes-skills/marketing/content-studio`, установл. копия в `%LOCALAPPDATA%\hermes\skills`). Миграции `0001` (таблица `hermes_agent.content_drafts`) и `0002` (поле `images`), применены в Docker Postgres `postgres16`/`mydb`. Helper без сторонних зависимостей: extract-url (текст + фото товара, `/stor` vs `/tild` фильтр), kie.ai text-to-image (обложка) и **image-to-image** (`gpt-image-2-image-to-image`, карточка товара из фото), сохранение в Postgres через `CONTENT_PSQL_COMMAND`.
**Ключевые находки/фиксы по ходу**: (1) секреты не доходили до песочницы `execute_code` — добавлены `required_environment_variables` во фронтматтер (`4174cc3`); (2) агент врал «база недоступна» при живом сохранении в Postgres — уточнён backend + `--require-postgres` (`ebe2eee`); (3) стратегия картинки уточнена с клиентом: свободный бриф → обложка YouTube с текстом (`b358372`), ссылка Tilda → маркетплейс-**карточка** из 1-го фото + 2-е фото как есть (`0ee5609`, `d88ad93`); (4) навык не триггерился на «создай пост … фото» без ссылки и уходил во встроенный FAL `image_generate` — расширено описание + правило в SOUL.md (`0c8cb46`).
**Верификация**: Kira подтвердила вживую в Telegram (2026-07-01): свободный бриф, ссылка Tilda (карточка «ВАЗА/LOVA CERAMICS» с реальными характеристиками + живое фото), 3 текста, сохранение черновика. Машинно: image-to-image на реальном фото lovaceramics вернул карточку (task `503c720c`); 2-картиночный черновик сохранён в Postgres (`images` длиной 2). ROADMAP stage 4 — все `[x]`.

2026-06-29 — Этап 3: Навык «код и тексты» (docx/csv) — tag `stage-3-done`.
Реализован локальный Hermes skill `business-documents`: исходник `hermes-skills/productivity/business-documents`, установленная копия `%LOCALAPPDATA%\hermes\skills\productivity\business-documents`. Helper без сторонних зависимостей: `.docx` собирается как Office Open XML, `.csv` пишется UTF-8 BOM; для отчётов поддержан section-based layout с заголовком, секциями, буллетами и таблицами внутри CSV.
**Верификация**: Анастасия/Kira подтвердила закрытие после живой Telegram-проверки stage 3 (команда «закрой этап 3», 2026-06-29). В логах gateway/agent за 2026-06-29 видна проверка CSV через Telegram: замечание по разметке CSV в 21:52, загрузка образца `Затирки.xlsx - Litokol Litochrom 1-6.csv` в 22:04, повторная проверка `business-documents` в 22:22-22:24. Машинная проверка артефактов: `kp_коммерческое_предложение_персональный_ии-агент_20260628_215641.docx` валиден как zip/Office XML (`word/document.xml` парсится), `installed_pretty_saka_kaka_report.csv` имеет UTF-8 BOM и 29 строк/23 непустые строки. ROADMAP acceptance stage 3 отмечен `[x]`.

2026-06-28 — Этап 2: Долгосрочная память — tag `stage-2-done`.
Используется **встроенная файловая память hermes** (БД не нужна): `memories/USER.md` (профиль) + `memories/MEMORY.md` (заметки агента), выборочно вшиваются в системный промпт каждой сессии. Проверено: сохранение факта → recall в новой сессии (после `/new`) — и в CLI, и в Telegram.
**Доработки**: (1) явные «запомни» сохранялись ненадёжно (curation + короткие сессии до порога флаша) → в SOUL.md добавлено правило сохранять немедленно; после фикса «любимый цвет зелёный» записался в USER.md и вспомнился. (2) DeepSeek подмешивал в память иероглифы (`不喜欢`) → правило «память только по-русски», старый артефакт почищен.
**На будущее**: при росте объёма можно подключить бэкенд (mem0/honcho), пока не требуется.

2026-06-28 — Этап 1: Живой мозг (текст/голос/фото/код/стиль) — tag `stage-1-done`.
**STT**: `stt.provider: groq` (whisper-large-v3-turbo, `GROQ_API_KEY`), local исключён. Подтверждено логом (19:27:33 «Прием, слышно?»). **Vision**: `auxiliary.vision.provider: openrouter` — фото анализируется по API (лог 19:28:45). **Стиль**: SOUL.md переписан под персону «кореш/друг» (на «ты», тёплый, дельный) + правило вывода кода (мелкий → блок в чат, крупный → файл-документ). **Модель**: DeepSeek V4 Pro через OpenRouter.
**Нюанс ключа**: Анастасия через дашборд `/env` сменила OpenRouter-ключ на свой dev-ключ (0870, инференсный, рабочий) вместо сгенерированного. На прод (этап 7) нужен инференс-ключ аккаунта клиента. Смена ключа в .env требует рестарта gateway (читается при старте).
**Верификация**: Анастасия подтвердила вживую все 6 критериев (текст, голос, фото, мелкий код в чат, крупный код файлом, стиль).

2026-06-28 — Этап 0: Каркас hermes-agent — tag `stage-0-done`.
Установлен hermes-agent v0.17.0 офиц. инсталлятором в `%LOCALAPPDATA%\hermes` (Python 3.11/Node/ffmpeg/Git Bash изолированно). Разобрано устройство репо → карта возможностей `docs/01_HERMES_CAPABILITIES.md`: Telegram/голос/память/cron/image_gen/social-media встроены; кастомно остаётся kie.ai-провайдер, docx/csv, навык контента, PostMyPost, контент-таблица+cron.
**Нетривиальное**: ключ OpenRouter из `доступы.txt` (cedd…) оказался provisioning-ключом — инференс им невозможен (401 «User not found»). Из него сгенерирован отдельный рантайм-ключ для агента (правильнее по безопасности; сообщить клиенту).
**Решение по ботам**: локально — dev-бот `@NEIRO_BRAIN01_BOT` + id Анастасии `2042819654`; прод `@Mironov_AgentBot` + Mika `5183294551` переключаем на этапе 7. Прод-значения в комментариях `.env`.
**Верификация**: модель ответила в CLI («На связи, работаю», DeepSeek V4 Pro); gateway поднят (polling), Анастасия подтвердила вживую — агент отвечает в Telegram. Поднят веб-дашборд на :9119.
**Заметка**: Playwright Chromium не доустановился (ECONNRESET) — браузерные инструменты не работают, доустановить позже: `npx playwright install chromium`.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены человеком.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
